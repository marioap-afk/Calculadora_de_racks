# Decision del Owner — I-47 (autorizacion de apertura sin fila previa y alcance de la primera sesion)

Estado: **registrada**. El dueno del repositorio autorizo **explicitamente** abrir I-47 y delimito su
primera sesion a **Discovery sin cambios de produccion**. Esta autorizacion es el caso (d) de
[WORKFLOW](../../WORKFLOW.md) seccion 2: sustituye **unicamente** la preexistencia de la fila en
ROADMAP, y no sustituye el reclamo atomico, la rama, el worktree, el contrato ni el bootstrap.

## Identificacion

| Campo | Valor |
|---|---|
| Iniciativa | I-47 — Variables de proyecto: fundacion de autoridad drawing-level (ID22A) |
| Rama | `architecture/project-variables-foundation` |
| Claim-Id | `73672933-f052-46af-b3c0-8091f0add299` |
| Commit de reclamo | `6e17bd5` (vacio), aceptado por el remoto como primer push sin force |
| Base | `origin/main` `306e18ed4676e5e96b54d59402c9a230efb137d3` |
| Fecha | 2026-09-08 |
| Decisor | Mario Perez, dueno del repositorio |
| Contrato | [`docs/initiatives/I-47-project-variables-foundation.md`](../../initiatives/I-47-project-variables-foundation.md) |
| ADRs | ninguno todavia — no se ha tomado ninguna decision de arquitectura |
| Gates abiertos | `owner-decision` (todo alcance posterior al Discovery) |

## Encargo, tal como lo emitio el dueno

Se transcribe porque delimita el alcance y porque el ID del dueno (**ID22A**) no tiene, hoy, ninguna
entrada previa en el repositorio: `git grep ID22` no devuelve nada en `docs/`. Ese ID vive en la
numeracion del dueno, no en el arbol.

1. Preflight completo desde `origin/main`, reportando local main, status, worktrees, ramas, stashes y
   operaciones Git incompletas.
2. Si I-47 sigue libre, **reclamar atomicamente** `architecture/project-variables-foundation` desde
   `origin/main` conforme a WORKFLOW, con su worktree y su claim.
3. **Bootstrap documental obligatorio** si corresponde.
4. **Completar Discovery sin cambios de produccion**: confirmar la autoridad drawing-level disponible
   para un unico `ProjectVariables` por DWG; auditar el descubrimiento y el redibujo de consumidores;
   y comparar `VerticalClearance`, `PalletTolerance` y `PalletDepth` como **vertical slice**.
5. Reportar SHAs, rutas/simbolos, hallazgos y riesgos.

Con dos exclusiones expresas, ambas en el encargo:

- **No implementar produccion.**
- **No disenar formulas ni propagacion rack a rack.**

## Lo que esta autorizacion **si** cubre

- Abrir la iniciativa sin fila previa en ROADMAP, con la obligacion de crearla en este bootstrap.
- Investigacion de solo lectura sobre todo el arbol.
- Producir el informe de Discovery **versionado en la rama**, y los documentos de gobierno de la
  propia iniciativa (contrato, fila, este registro).

## Lo que esta autorizacion **no** cubre

- Cualquier cambio bajo `src/`, `tests/` o `assets/`.
- Elegir el mecanismo de autoridad drawing-level. El Discovery **describe** las opciones reales; la
  eleccion es del dueno y, por alcance, probablemente exige ADR.
- Disenar formulas, aritmetica o unidades derivadas de las variables auditadas.
- Disenar herencia, copia o imposicion de valores **entre racks**.
- Definir migracion de dibujos existentes, formato de persistencia, UI o comando nuevo.
- Corregir «de paso» cualquier defecto que el Discovery destape.

## Preguntas abiertas para el dueno (planteadas en el Discovery)

Se registraron aqui, sin responderlas, porque el Discovery no podia decidirlas. **Las cuatro quedaron
resueltas en el Gate C**; se conservan literales y se marca cual decision las cierra.

1. **Que hace un DWG heredado** que no tiene registro de variables: nace con valores por defecto,
   nace vacio, o el comando falla hasta que alguien lo cree. — **RESUELTA por C-1.**
2. **Que manda cuando un rack ya guardado discrepa** del valor de proyecto: gana el proyecto, gana el
   rack, o el rack queda marcado como desactualizado sin tocarse. — **RESUELTA por C-2.**
3. **Alcance de la lista de variables**: las tres del vertical slice son una **muestra**, no la lista
   definitiva; el dueno no ha fijado la lista. — **RESUELTA para ID22A por C-5** (el slice es
   Selectivo). La lista completa sigue sin fijarse, pero **fuera** de ID22A.
4. **Si el objetivo incluye redibujo masivo** de lo ya colocado, o solo afecta a lo que se dibuje
   despues. — **RESUELTA por C-3.**

## Decisiones vinculantes del Gate C (2026-09-08)

Emitidas por el dueno al abrir el Gate C, sobre el SHA `9e25d5291daa13c4846112241be6e52526429a47`.
**No se reabren.** La Proposal V1 las trata como **premisas**, no como opciones a comparar.

| # | Decision |
|---|---|
| **C-1** | Un DWG **sin** `ProjectVariables` tiene un **registro vacio**. **No hay migracion**, y los **literales existentes se preservan** tal cual. |
| **C-2** | Una propiedad es **`Literal`** o **`ProjectVariableReference`**. Cuando es referencia, **la referencia gobierna el valor efectivo**. |
| **C-3** | Cambiar una variable **propaga y redibuja a todos sus consumidores en UNA operacion**. |
| **C-4** | **RACKDUPLICAR conserva el mismo `VariableId`.** |
| **C-5** | **ID22A no unifica** el `ClearHeight` de otros sistemas. El vertical slice es **Selectivo**. |
| **C-6** | **`defaults.json` convive** y **no** se convierte en `ProjectVariables`. |

**Sigue diferida, por decision expresa del dueno:** la supervivencia del registro a **WBLOCK y a
copiar/pegar entre dibujos**. Es cuestion **diferible**, no un requisito de ID22A.

### Efecto sobre el alcance

El Gate C **no** autoriza implementacion. Autoriza escribir una **Proposal V1 documental** que compare
alternativas y recomiende un contrato bajo esas seis premisas, sin tocar `src/` ni `tests/`. El gate
`owner-decision` **sigue abierto** para el contrato que la Proposal recomiende. **No se pidio Architect
Review todavia.**

## Decisiones vinculantes del Gate C2 (2026-09-08)

Emitidas sobre el SHA `83a2cdb824560c83fbd7e78ca03aa3690cc80474` (Proposal V1). **No se reabren.**
Cuatro son **definiciones** del dueno; cinco son **correcciones de hecho** a la Proposal V1, y dos de
esas cinco **invalidan** una afirmacion de V1 en vez de matizarla.

| # | Decision | Tipo |
|---|---|---|
| **C2-1** | **ID22B = formulas/expresiones entre variables.** No se disenan en ID22A | definicion |
| **C2-2** | **ID21 = referencias a propiedades de racks**, previstas como **`RackId` + `PropertyId`**. No se disenan en ID22A | definicion |
| **C2-3** | Se separan tres conceptos: **`ProjectVariable`** (VariableId, Name, Type, definicion de valor), **`PropertyValue<T>`** (`Literal(T)` \| `ProjectVariableReference(VariableId)`) y **`PropertyId`** estable. Para el slice, `selective.verticalClearance`. ID21 podra anadir `RackPropertyReference(RackId, PropertyId)` **sin implementarlo** | definicion |
| **C2-4** | La referencia persistida es **tipada** (`PropertyId → { kind, variableId }`); **no** `slot → string`. ID22A acepta **solo** `kind: projectVariable`. El escalar legacy se conserva como literal | definicion |
| **C2-5** | **`[JsonExtensionData]` anadido ahora NO protege frente a una version pre-I-47 ya compilada.** Hay que distinguir *backward compatibility* de *downgrade / forward round-trip*, y no atribuirle una garantia falsa | **correccion: invalida V1** |
| **C2-6** | **`SystemBlockWriter.RedrawInPlace` commitea por bloque**, asi que el lote **no** es todo-o-nada. El contrato exige preflight completo antes de mutar, coherencia conjunta, **commit unico o rollback real**, y un solo `Regen` | **correccion: invalida V1** |
| **C2-7** | **Un round-trip del Store no prueba `SelectiveKindHandler.RestampDesign`.** Hay que verificar por **comportamiento** que RACKDUPLICAR cambia `RackId`/`Name` y conserva el `VariableId`, preferentemente extrayendo la transformacion a codigo puro | correccion |
| **C2-8** | La **transferencia/fusion de variables entre dibujos (H3) no pertenece a ID22B**: queda como futuro **separado** | correccion |
| **C2-9** | **Condicion de Consensus Freeze**: el contrato de iniciativa y la fila de ROADMAP se actualizan **antes de produccion** para dejar de presentar UI/comando como fuera de alcance si forman parte del plan final | proceso |

Ademas, precision de API sin cambio de decision: el acceso al NOD se escribe
**`Database.NamedObjectsDictionaryId` → `DBDictionary`**. La recomendacion de usar el NOD **no cambia**.

### Efecto sobre el alcance

El Gate C2 **no** autoriza implementacion y **no** pide Architect Review. Autoriza **Proposal V2**
([`docs/initiatives/I-47-proposal-v2.md`](../../initiatives/I-47-proposal-v2.md)), que sustituye a V1 y
corrige **solo** estos nueve puntos. El gate `owner-decision` **sigue abierto**.

## Decisiones vinculantes del Gate C3 (2026-09-08)

Emitidas sobre el SHA `e08431fb0104fd1ad3c15f11705ec9ac9747de55` (Proposal V2). **No se reabren.**
Con este gate **el contrato deja de tener decisiones de producto o arquitectura pendientes**.

| # | Decision | Tipo |
|---|---|---|
| **C3-1** | **D-07 = F2.** **F3 se RECHAZA**: una version vieja puede *preservar* el binding en el sobre pero **no entiende su semantica**, asi que podria editar y redibujar usando el **literal** y dejar geometria **incoherente** con la autoridad que una version nueva resolvera despues | decision |
| **C3-2** | F2 se corrige a **promocion de schema PEGAJOSA**: un documento `1.x` **nunca vinculado** puede seguir escribiendose `1.x`; introducir `PropertyValues` **promueve a `2.0`**; una vez almacenado como `2.x` **NO vuelve a `1.x`** aunque se elimine el ultimo binding; **desvincular materializa el literal y elimina la referencia, pero no degrada el schema**; los minors posteriores se preservan segun `SchemaVersionPolicy` | correccion |
| **C3-3** | **D-10 = F1.** Borrar una variable con consumidores **se bloquea** y **se listan** los consumidores. «Desvincular todos materializando + borrar» puede existir **solo** como accion explicita separada | decision |
| **C3-4** | `RedefineSystemBlock` usa la **`Transaction` del llamador**; se mantiene **transaccion unica** para `ProjectVariables` + geometria + payloads | correccion |
| **C3-5** | **`BlockLibraryImporter.EnsureForPlan` NO forma parte del preflight ni del lote** de propagacion: puede mutar el `Database` **fuera** de la transaccion. La propagacion **verifica** que las definiciones requeridas ya existan; **si falta alguna, aborta antes** de cambiar la variable o los consumidores; **no se importa ni se repara** la biblioteca como efecto colateral. Purga **acumulada post-commit** y **un solo `Regen`** | correccion |
| **C3-6** | La **viabilidad de la transaccion unica deja de ser pregunta de contrato**. La validacion real en AutoCAD se mantiene como **gate de implementacion / Owner Validation**, **no** como condicion previa de Consensus Freeze | proceso |
| **C3-7** | Conforme a **WORKFLOW seccion 2**: el **contrato de iniciativa** si debe reflejar el alcance final **antes de produccion**; **`ROADMAP.md` NO se modifica en un gate intermedio** y se actualiza **al integrar/cerrar**. ROADMAP **sale** del Consensus Freeze y pasa al **checklist de cierre** | proceso |
| **C3-8** | Se **retiran** de «preguntas abiertas»: F2 vs F3, D-10, la viabilidad conceptual de la transaccion unica y asignar numero a H3. El **coste de propagacion** queda como **riesgo/metrica de implementacion**, no como decision de arquitectura | proceso |

### Efecto sobre el alcance

El Gate C3 **no** autoriza implementacion y **no** pide Architect Review. Autoriza **Proposal V3**
([`docs/initiatives/I-47-proposal-v3.md`](../../initiatives/I-47-proposal-v3.md)), que sustituye a V2 y
corrige **solo** estos ocho puntos.

**Estado del gate `owner-decision`:** sigue **abierto**, pero su naturaleza cambia. Ya **no** espera que
el dueno elija entre alternativas —no queda ninguna viva—: espera su **aceptacion formal** del contrato,
que se materializa al pasar el ADR de D-19 de `propuesto` a `aceptado`.

## Trazabilidad

Este documento es el **registro verificable** que exige el proceso para una apertura por caso (d). Si
el dueno emite despues decisiones vinculantes sobre el alcance, se anaden a este archivo numeradas,
sin reescribir lo ya registrado.
