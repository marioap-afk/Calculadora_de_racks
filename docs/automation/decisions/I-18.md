# Decisión del Owner — I-18 Push Back (gate owner-decision: PB-0)

Estado: **aprobada (PB-0)**. Resuelve el gate `owner-decision` de I-18 aportando los **bloques DWG**, las
**filas de catálogo** y las **decisiones funcionales** que faltaban. Autoriza el arranque de **I-18a**
(núcleo puro). **No** autoriza I-18b/I-18c (registro, editor, dibujo, guía), que requieren su propia
revisión.

## Identificación

| Campo | Valor |
|---|---|
| Iniciativa | I-18 — Push Back |
| Rama | `feature/push-back` |
| Fecha | 2026-07-22 |
| Decisor | Owner / Orquestador Maestro (autorización en la corrida de implementación de I-18a) |
| Contrato | [`docs/initiatives/I-18-push-back.md`](../../initiatives/I-18-push-back.md) |
| Gate resuelto | `owner-decision` (PB-0: bloques, catálogo y decisiones funcionales) |
| Registro verificable | Este documento + los CSV de catálogo de la rama + el prompt de orquestación de esta sesión |

## PB-0.1 — Catálogo y bloques (provistos por el Owner)

Pieza nueva **`LARGUERO_ESCALON_TROQUEL_REDONDO`** (append-only; no cambia datos de otros sistemas):

- `secciones.csv`: rol `LARGUERO`, familia `Larguero`, peraltes `3;3.5;4;4.5;5;5.5;6`, ménsula
  `MENSULA_TROQUEL_REDONDO_CAL_10`.
- `blocks.csv`: tres vistas — `..._PLANTA`, `..._FRONTAL`, `..._LATERAL`.
- `connection-layout.csv`: `INICIO_PERFIL` (PLANTA, FRONTAL) + `INICIO_IZQUIERDO`/`INICIO_DERECHO`
  (LATERAL). `connection-points.csv` **sin cambios** (`INICIO_IZQUIERDO`/`INICIO_DERECHO`/`INICIO_PERFIL`
  ya existen).
- Validador I-19: **sin errores nuevos** (baseline conocido intacto: 1 error `TROQUEL_TOPE`, 2 avisos
  `TARIMA_GENERICA`).

## PB-0.2 — Decisiones funcionales autorizadas

Push Back sigue la estructura del **Dinámico**, pero opera como **LIFO**: entrada y salida en el **mismo
extremo** (bajo, izquierda).

1. **Estructura longitudinal:** reutiliza el patrón del Dinámico —cabeceras, separadores, postes
   derivados, largueros intermedios de ajuste infinito, frentes con distinta cantidad de fondos,
   `DepthStartPosition`, frentes de diferente longitud sobre la misma estructura base. **No** limitar a
   sistemas rectangulares uniformes.
2. **Pendiente:** exacta al Dinámico — extremo **bajo a la izquierda**, **alto a la derecha**, pendiente
   fija **7/16" por pie**, mismo cálculo y orientación. El extremo bajo izquierdo es entrada/salida.
3. **Largueros de extremo (por frente y nivel):** extremo bajo = un **IN/OUT** (`LARGUERO_IN_OUT_C6`,
   igual al Dinámico); extremo alto = **`LARGUERO_ESCALON_TROQUEL_REDONDO`** (peraltes `3..6`,
   **predeterminado explícito 3.5**, no por ser la primera fila del CSV; contactos `INICIO_IZQUIERDO`/
   `INICIO_DERECHO`). Su longitud transversal sigue la misma regla que el IN/OUT (BFR, nº de posiciones,
   tolerancias del frente).
4. **Elevaciones y tangencias:** ambos largueros de extremo se ajustan al **troquel más cercano**
   (referencia = primer troquel del poste, paso **2"**, misma regla de snap del Dinámico). La línea de
   cama es tangente al punto de conexión del IN/OUT, a `INICIO_IZQUIERDO`/`INICIO_DERECHO` del larguero
   posterior y al punto correspondiente de cada larguero intermedio. Reutiliza el principio de
   `DynamicIntermediateBeamLateralBuilder` (el punto físico toca la misma línea de origen del riel); **no
   duplicar la fórmula**.
5. **Cama Push Back:** componer `FlowBedLateralBuilder` con `FlowBedType.Pushback` (rodillos + tope propio
   de cama, **sin frenos**). **Longitud comercial = longitud estructural completa del frente**
   (`front.EndX − front.StartX`, o su equivalente canónico: suma de cabeceras y separadores del rango
   resuelto). **No** aplicar el descuento de 4" del Dinámico (`FlowBedLengthClearance`).
6. **Seguridad posterior:** Push Back **no** lleva guías de entrada. En el lado de entrada/salida pueden
   existir las familias de seguridad aplicables **excepto guías**. En el extremo posterior: **no** se
   colocan las familias normales del Dinámico; se coloca **un larguero tope por frente y nivel**,
   **activo por defecto**, **desactivable por celda**, reutilizando la regla canónica del Selectivo
   (`SAQUE`, longitud, elevación respecto del larguero, snap a troquel, proyecciones coherentes, conteo
   físico único en BOM). El estado por defecto materializa **todas** las celdas posteriores como activas
   **sin** persistir una lista completa de celdas positivas: se persisten **excepciones/desactivaciones**
   (patrón `OffCells` de `SelectiveTopeConfig`), garantizando round-trip.

## Alcance aprobado y límites

Autoriza **I-18a** (núcleo puro Domain/Application + persistencia + BOM + pruebas) reutilizando contratos
compartidos. **No** autoriza registrar Push Back en `SystemRegistry`/`KindHandlerRegistry`/
`EditorModuleRegistry`, ni editor/handler/dibujo/comandos/guía (I-18b/I-18c). Si agregar Push Back
exigiera **alterar la lógica** de Selectivo o Dinámico, el ejecutor **se detiene y reporta** el contrato
compartido faltante antes de forzar el cambio.

## Efectos

| Tema | Estado posterior |
|---|---|
| `requires_owner_decision` (PB-0) | Resuelto → `approved` para I-18a. |
| Estado de la iniciativa | `implementing` (I-18a); I-18b/I-18c pendientes de revisión. |
| `HANDOFF.md` / `ROADMAP.md` | **No** se modifican (se tocan en la integración, WORKFLOW §4.5.4). |
| Integración a `main` | No procede (operación manual y serializada del Owner). |

## Addendum PB-VAL-06 (2026-07-24) — Push Back no admite parrillas

PB-0.2 item 6 autorizó, en el extremo bajo, «las familias de seguridad aplicables **excepto guías**». La
reanudación de I-18b (instrucción directa del Owner) **precisa esa regla**: Push Back tampoco admite
**parrillas** (PARRILLA). La autoridad canónica de seguridad excluye **GUIA y PARRILLA** en todo el sistema
(UI, resolver, sistema, planes, dibujo y BOM). El tratamiento de una parrilla persistida por un documento
previo es **idéntico al ya aceptado para GUIA**: se lee sin error y se descarta en build (no reaparece en el
snapshot ni en ninguna vista/BOM), **sin migración destructiva** — por lo que no requiere una decisión nueva
sobre advertencia o migración. Esta aclaración no amplía el alcance de I-18b ni toca otros sistemas.

## Addendum 2026-07-24 (tarde) — decisiones del Owner sobre geometría y ventana

Tras el smoke visual sobre `10d8eeb`, el Owner emitió seis decisiones que **sustituyen toda interpretación
previa** (incluidas las reglas de PB-VAL-02 y PB-VAL-05 de las rondas anteriores).

### Geometría (implementada en esta corrida)

1. **Mate del IN/OUT bajo.** El larguero IN/OUT bajo se coloca para que su `TROQUEL_CAMA` coincida
   **exactamente** con el `TROQUEL_IN` del riel. Se **elimina de producción** el desplazamiento
   `BedOriginOffset` del larguero bajo: la cama se resuelve desde ese mate y el larguero **no** se mueve
   hacia la línea del origen del riel. Revierte la corrección de la ronda 2.
2. **Tangencia del larguero posterior.** Es el `LARGUERO_ESCALON_TROQUEL_REDONDO` —debajo del tope— el que
   se coloca para que su `INICIO_IZQUIERDO`/`INICIO_DERECHO` transformado **correcto** sea tangente a la
   línea del origen físico de la cama. El borde correcto se elige por geometría (la línea sube hacia +X, así
   que el contacto ocurre en el borde de mayor X en mundo), no por una constante izquierda/derecha. Se
   conservan pendiente, longitud completa, snap, peralte por celda, intermedios y el resto de la geometría
   aprobada; la cama se resuelve desde las colocaciones crudas y queda bit-idéntica.
3. **Tope posterior.** Su orientación estaba **invertida** en `10d8eeb` y se invierte (las elevaciones se
   dibujan sin espejo; PLANTA conserva su orientación en planta, aprobada). Su origen se ancla al **eje
   vertical del `TROQUEL_SEPARADOR` del poste**, resuelto con el peralte del poste, y PLANTA usa el
   `TROQUEL_SEPARADOR` medido en PLANTA. No se usan los puntos del larguero posterior ni un fallback a
   `placement.X`: sin punto medido no hay ancla. Se conservan +4", snap, SAQUE, LONGITUD, OffCells y BOM.

### Ventana y preview (registradas, **no** implementadas en esta corrida)

4. **Topes solo en Seguridad.** Eliminar `BulkToolsExpander`, `CellSelectionMatrix`, `TopeMatrix`, los
   botones «Topes: todos/ninguno», los hints y el marcado de topes en las tarjetas, `RearTopeActiveCheck` y
   «SAQUE topes» de la captura general; los alcances de celda no deben aplicar topes ocultamente. Toda la
   configuración del tope posterior vive en Seguridad, reutilizando `SafetyTopeGridWindow` /
   `SelectiveSafetyWindow` por adaptador (proyectar `PushBackRearTopeConfig` a la rejilla al abrir; recuperar
   SAQUE y OffCells al aceptar), sin que el TOPE entre como seguridad normal del extremo bajo.
5. **Panel resaltado frente/celda.** Un panel al patrón visual del Dinámico que separe «Frente seleccionado»
   (posiciones, niveles, fondos, inicio, alto inicial, con aplicación por frente / frentes seleccionados /
   todos) de «Celda seleccionada» (tarima, claro, largo, largueros y peraltes, con los alcances vigentes),
   manteniendo la matriz superior limpia.
6. **Preview rico.** Sustituir el preview simplificado por uno con la riqueza del Dinámico en las cuatro
   vistas, reutilizando o **extrayendo** infraestructura común del renderer Dinámico — nunca copiándolo ni
   manteniendo dos painters divergentes; cualquier extracción que toque el Dinámico debe ser mecánica y
   probada como salida idéntica.

Los puntos 4-6 quedan **abiertos**: no se implementaron en esta corrida y ninguna parte de ellos se dejó a
medias en el árbol (ver el estado de I-18 para el motivo y el alcance pendiente).

### Estado de las decisiones 4-6 (actualización)

- **4 (topes solo en Seguridad) e 5 (panel resaltado frente/celda): IMPLEMENTADAS.** Los controles
  redundantes desaparecieron de XAML y code-behind; la multiselección por clic/Ctrl+clic de las tarjetas es
  ahora la única superficie de selección; el tope salió de `PushBackEditorValues`, así que ningún alcance de
  celda puede transportarlo; Seguridad edita el tope reutilizando `SafetyTopeGridWindow` por adaptador; y
  `PushBackSafetyAuthority` rechaza el TOPE como seguridad normal (`IsRearStop`), de modo que una sola
  autoridad posee la pieza.
- **6 (preview rico): ABIERTA.** No se implementó y nada quedó a medias. Es una extracción con doble
  consumidor (Dinámico + Push Back) que exige pruebas de equivalencia de escena antes/después sobre el
  Dinámico; merece su propia corrida.

### Corrección del flujo integrado de Seguridad (2026-07-24, tarde)

El flujo introducido con la decisión 4 tenía un **defecto real**: tras aceptar `SelectiveSafetyWindow`, el
handler re-filtraba la lista de ENTRADA (`safetySelections`) en lugar de leer `dialog.Result`, de modo que
todo lo que el usuario cambiaba —botas, protectores laterales, desviadores y defensa— se perdía en silencio
y nunca llegaba al diseño ni al BOM. Corregido: se toma `dialog.Result` y se pasa por
`PushBackSafetyAuthority.Authorize`, que copia en profundidad cada selección, la restringe al extremo bajo y
rechaza GUIA, PARRILLA y TOPE. La **cancelación** pasa a tener semántica explícita: cancelar el diálogo de
seguridad abandona todo el paso (ni siquiera abre el de topes); cancelar solo el de topes conserva la
seguridad aceptada y deja la configuración del tope intacta.

### Estado de la decisión 6 (actualización)

Sigue **abierta**, pero ya no desde cero: la **caracterización del renderer Dinámico** está hecha y verde, y
congela su escena real (primitiva a primitiva, en orden de dibujo, con color, grosor y dash), además de fijar
que el renderer es determinista y que cada vista tiene su propia firma. Con esa red puesta, la extracción
posterior puede **demostrarse** equivalente en vez de afirmarse.

### Decisión 6 — COMPLETADA (2026-07-24)

Se extrajo del renderer del editor Dinámico una infraestructura de preview **compartida**, que ahora consumen
los dos editores:

- **`EditorPreviewPalette`** — los 17 brushes por rol, con los colores originales del Dinámico, en una sola
  definición.
- **`EditorPreviewSurface`** — la transformación world→canvas del propio Dinámico, su `Fit` con márgenes, el
  `Unmap` para el hit-test y el vocabulario de primitivas (líneas, rectángulos, etiquetas, etiquetas
  centradas, dash) más `LocalToWorld`.
- **`EditorPreviewParts`** — cómo se dibuja cada familia dada una geometría **ya resuelta**: largueros de
  carga, soportes intermedios, seguridad, camas/rodillos/frenos/topes de cama, estructura por rol
  (postes, placas, horizontales, cierres, diagonales, separadores), spans de viga, topes posteriores,
  tarimas y línea de piso.

La infraestructura es **neutral** (ADR-0019 §3): no conoce `RackSystemKind` ni tiene ramas por sistema, y
**consume** geometría resuelta — no recalcula pendiente, snap, mates, longitudes, SAQUE ni peraltes, y no
muta planes.

**Equivalencia del Dinámico, medida.** La firma SHA-256 de su escena real (primitiva a primitiva, en orden de
dibujo, con geometría, color, grosor y dash) es `B7C0D1CA…` sobre **736 primitivas** antes de extraer y la
misma después, comprobado ejecutando el mismo probe contra el código pre-extracción y contra el migrado. El
valor queda fijado como pin, y las 4 pruebas de caracterización previas siguen pasando **sin cambiar sus
expectativas**.

**Push Back** pinta sus cuatro vistas con esa misma infraestructura; `PushBackPreviewModel` queda **reducido**
a adaptador semántico de solo lectura (seam de pruebas) y **fuera** de la ruta de dibujo, verificado por
prueba.

## Addendum 2026-07-24 (final) — tope dentro de Seguridad, anclaje por vista y preview diferido

Estas decisiones del Owner **sustituyen cualquier interpretación previa**.

### 1. Tope visible dentro de Seguridad

El tope posterior se configura dentro de la misma experiencia visible de «Elementos de seguridad», como una
sección propia: encabezado **«Topes posteriores»**, su estado actual y un botón **«Configurar…»** que reutiliza
`SafetyTopeGridWindow` y edita exclusivamente `PushBackRearTopeConfig.Saque` y `.OffCells`.

`SelectiveSafetyWindow` gana un punto de extensión **neutral** (`extraSection`): renderiza el elemento que le
entreguen sin inspeccionarlo, así que sigue sin conocer Push Back. La sección trabaja sobre una **copia**; al
aceptar la ventana principal se aplican en **una sola operación** la seguridad autorizada y la configuración
del tope, y al cancelarla no se modifica **ninguno** de los dos estados. El diálogo del tope se abre **solo**
desde su botón visible: nunca automáticamente después de Aceptar. El tope no vuelve a ser
`SelectiveSafetySelection`, y GUIA y PARRILLA siguen fuera.

### 2. Anclaje del larguero tope, por vista

Auditadas las filas reales de `connection-layout.csv` — todas las requeridas existen:

| Vista | Punto | Fila |
|---|---|---|
| LATERAL | `TROQUEL_SEPARADOR` | `0.875, 2.1563` |
| FRONTAL | `TROQUEL_TOPE` | `0 + 0.5·PERALTE, 2.1563` |
| PLANTA | `TROQUEL_TOPE` | `0.875, 0 + 0.5·PERALTE` |

- **LATERAL**: X sobre el eje vertical del separador y elevación resuelta **desde ese mismo punto**, con la
  distancia vertical al tope en múltiplos enteros de 2" (los +4" aprobados caben en la retícula porque son dos
  pasos). Antes se leía `TROQUEL_LARGUERO` **en vista FRONTAL** — punto y vista equivocados; como el poste no
  publica esa fila en LATERAL, la retícula quedaba basada silenciosamente en 0.
- **FRONTAL posterior**: anclado por el `TROQUEL_TOPE` del poste en esa vista, cuya X sigue el peralte.
- **PLANTA**: anclado por `TROQUEL_TOPE` en PLANTA, resuelto con el peralte real, y orientación **invertida**.

Ninguna vista usa `TROQUEL_LARGUERO` y ninguna cae al punto de inserción: sin fila medida no se emite pieza.
Pieza, SAQUE, LONGITUD, OffCells, conteo del BOM y los +4" se conservan.

### 3. Preview — diferido

Los previews **siguen siendo insatisfactorios** para el Owner. **No son bloqueantes para I-18** y su
estandarización completa se **difiere a una iniciativa transversal futura**. En esta corrida no se tocaron la
infraestructura compartida, el renderer Dinámico, el renderer Push Back ni las pruebas de riqueza visual.
**El preview NO ha sido aprobado visualmente.**

## Bloqueo 2026-07-25 — RESUELTO por aclaración del Owner (ver addendum siguiente)

> **Resuelto el mismo día:** el Owner aclaró que `LARGUERO_ESCALON_TOPE_DE_3` **mate por su ORIGEN**, así que no
> falta ninguna fila: el punto local del tope es su propia inserción. El bloqueo que sigue queda como registro
> histórico de la auditoría.

### Registro del bloqueo (histórico)

El Owner pidió que el tope se coloque haciendo coincidir **físicamente**, en coordenadas mundiales:

```
World(LARGUERO_ESCALON_TOPE_DE_3.TROQUEL_TOPE) == World(POSTE….TROQUEL_TOPE)
```

en FRONTAL y en PLANTA, y ordenó **detenerse** si el bloque del tope no publica todavía ese punto.

**Auditoría de `assets/catalogs/connection-layout.csv` (y de las 5 copias de `bin/`):**

- El **poste** SÍ publica su lado del mate:
  - `POSTE_OMEGA_ATORNILLABLE_CON_TROQUEL_GOTA_DE_AGUA,TROQUEL_TOPE,FRONTAL,0,0.5,PERALTE,2.1563,0,`
  - `POSTE_OMEGA_ATORNILLABLE_CON_TROQUEL_GOTA_DE_AGUA,TROQUEL_TOPE,PLANTA,0.875,0,,0,0.5,PERALTE`
- El **bloque del tope no publica NINGUNA fila** en `connection-layout.csv`, en ninguna vista. Existe en
  `blocks.csv` (FRONTAL/LATERAL/PLANTA) pero no tiene un solo punto de conexión medido.

Sin el lado del tope no hay mate que resolver: calcular el insertion point exigiría el punto local, y usar el
origen de inserción o un offset estimado es exactamente lo que la decisión prohíbe.

**Filas faltantes, en el formato exacto del CSV**
(`pieceId,connectionPointId,view,localX,localXPorParam,paramX,localY,localYPorParam,paramY`):

```
LARGUERO_ESCALON_TOPE_DE_3,TROQUEL_TOPE,FRONTAL,<localX>,<localXPorParam>,<paramX>,<localY>,<localYPorParam>,<paramY>
LARGUERO_ESCALON_TOPE_DE_3,TROQUEL_TOPE,PLANTA,<localX>,<localXPorParam>,<paramX>,<localY>,<localYPorParam>,<paramY>
```

Los valores deben medirse en el bloque real (`LARGUERO_ESCALON_TOPE_DE_3_FRONTAL` y `…_PLANTA` de
`Base_de_datos_AutoCAD_V.0.dwg`), desde el origen de inserción del bloque hasta el centro de su troquel de
tope. Si ese troquel se desplaza con algún parámetro dinámico (p. ej. el peralte), se declara con las columnas
`…PorParam`/`param…`, igual que hace el poste en FRONTAL/PLANTA.

**No bloquea el LATERAL**, que el Owner aprobó: allí la regla es de eje y retícula sobre `TROQUEL_SEPARADOR`,
no un mate punto-a-punto, y queda intacta.

**Estado de las dos vistas afectadas:** FRONTAL posterior y PLANTA siguen como en `90d3b3a` — con el anclaje
por el punto del poste, que el Owner midió insuficiente. No se cambió su geometría en esta corrida.


## Addendum 2026-07-25 (final) — el tope mate por su ORIGEN contra el TROQUEL_TOPE del poste

**Aclaración del Owner que resuelve el bloqueo anterior:** el bloque `LARGUERO_ESCALON_TOPE_DE_3` **hace mate
con su origen**. Su punto de inserción ES su punto de mate, y por eso el bloque no publica —ni necesita— un
punto de conexión propio en `connection-layout.csv`. Los puntos `TROQUEL_TOPE` son del **poste**.

Colocar el tope es, entonces: **poner su origen exactamente sobre el `TROQUEL_TOPE` del poste, en coordenadas
mundiales**, en la vista correspondiente.

### El defecto real

Ambas vistas partían de la inserción del **larguero**, no de la del **poste**:

- **PLANTA** nunca llegó a usar el ancla: `PushBackSystemPlantaBuilder` construye su tope *inline* y usaba
  `instance.Insertion.X / .Y` del larguero **literalmente**. Por eso el Owner lo seguía viendo en
  `TROQUEL_LARGUERO`: lo único que cambió en `90d3b3a` fue el espejo.
- **FRONTAL posterior** partía de la inserción del larguero desplazada por el punto local, que no es el mismo
  datum que el poste.

### La corrección

`PostMateWorld` resuelve el punto mundial destino tomando la instancia de **POSTE del propio plan** (la más
cercana al larguero servido) y transformando el punto medido del poste por la colocación de ese poste, mirror
incluido. Sobre eso:

- **PLANTA**: coincidencia **exacta** en X e Y (no hay elevación). Verificado: `dx = 0`, `dy = 0`.
- **FRONTAL posterior**: coincidencia **exacta en X** (la columna de troqueles de tope del poste) y la Y sobre
  esa misma columna, a un número entero de pasos de 2" — porque el poste tiene una columna de agujeros cada 2"
  y el rise-and-snap aprobado (+4") elige el del nivel. Verificado: `dx = 0`, `dy = 22"` y `94"`.

Sin fallback: sin fila medida o sin poste en el plan no se emite la pieza.

**Alcance:** el golden mueve **solo** `frontal-posterior` y `planta` — las dos vistas rechazadas. El **LATERAL
queda byte-idéntico** y el BOM tampoco cambia. Se conservan pieza, SAQUE, LONGITUD, +4", OffCells y BOM.
