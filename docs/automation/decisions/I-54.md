# I-54 — Registro de decisiones: consenso técnico sobre Proposal V5 y ADR-0039 propuesto

## 1. Identificación

```text
Initiative           = I-54 — ID24 Custom Properties Foundation
Branch               = architecture/propiedades-personalizadas
Proposal             = V5 @ 26ca923492576185b753d6dbf2a852969df2accf
Proposal blob        = a75444702ad354b35b67bfbbf5bb955913a02c81
ADR                  = ADR-0039 — PROPOSED
Owner                = Mario Pérez
Owner ADR Acceptance = PENDING
Gate                 = G2-FREEZE (preparación documental)
Date                 = 2026-09-13
```

Documento congelado: [`docs/initiatives/I-54-proposal-v5.md`](../../initiatives/I-54-proposal-v5.md), tal como existe
**en ese SHA exacto**. Código base auditado por el Discovery y por las Proposals:
`46fcac2b071929d2bd5b07aa28373941417f74a8` (`BASE_SHA`). Los archivos de código citados son idénticos en
`f8deb675c6d1ef0e64693b157d69c4cc170d7b24`, el `origin/main` sobre el que la rama está rebasada desde G2G.

Es el primer registro de decisiones de I-54. La iniciativa se abrió por el caso (d) de
[WORKFLOW](../../WORKFLOW.md) §2 y su contrato no lo exigía hasta ahora. Se crea en G2-FREEZE para dejar versionados
el consenso técnico y el ADR propuesto, como hicieron I-47, I-49 e I-50, y el contrato pasa a declararlo en
`decision_paths`.

---

## 2. Consenso técnico

```text
Coordinator = AGREED on Proposal V5 @ 26ca923492576185b753d6dbf2a852969df2accf
Architect   = AGREED on Proposal V5 @ 26ca923492576185b753d6dbf2a852969df2accf   (G2J)

Technical Consensus = REACHED
```

CI de V5: run `34747220760`, `head_sha = 26ca923492576185b753d6dbf2a852969df2accf`, `conclusion = success`, con los
cuatro jobs en verde (Tests Domain + Application, Build UI, UI Tests y Build Plugin without AutoCAD).

La revisión final del Arquitecto (G2J), limitada a AR-54-V4-01 / C-F3, cerró así:

```text
Q-1 F-14b description             = VERIFIED
Q-2 no global atomicity claim     = VERIFIED
Q-3 D-22.10 exclusive guarantee   = VERIFIED
Q-4 D-13 / RP-11 alignment        = VERIFIED
Q-5 future F-14 wording           = VERIFIED
Q-6 scope integrity               = VERIFIED

AR-54-V4-01 = CLOSED
AR-54-V3-01 = CLOSED
AR-54-V3-02 = CLOSED
AR-54-V2-03 = CLOSED
AR-54-04    = CLOSED

RD-04 = AGREE
RD-07 = AGREE
ADR REQUIRED    = YES
ADR SCOPE       = AGREED
Authority order = AGREED

New findings              = NONE
Required Proposal changes = NONE
G2J Architect Review      = CLOSED
```

**El consenso se alcanzó tras cinco rondas de revisión, cada una sobre un SHA concreto.** Los veredictos de las tres
primeras pertenecen a sus SHAs **pre-rebase**; los equivalentes rebasados de G2G no son SHAs revisados (Proposal V5
§2.2).

| Proposal revisada | Coordinator | Architect | Hallazgos |
|---|---|---|---|
| V1 `7c197af` (post-rebase `728d11f`) | — | AGREED WITH CHANGES (G2B) | 0 BLOCKER, 5 MATERIAL, 13 MINOR; cambios V2-1..V2-22 |
| V2 `36c337b` (post-rebase `2d0d666`) | acepta C-1..C-8 | AGREED WITH CHANGES (G2D) | 2 MATERIAL y 4 MINOR nuevos; AR-54-04 reabierto; cambios C-1..C-8 |
| V3 `5d25da8` (post-rebase `ff98b9e`) | AGREED | AGREED WITH CHANGES (G2F) | 2 MINOR: AR-54-V3-01 y AR-54-V3-02 |
| V4 `8bc991c` | AGREED | AGREED WITH CHANGES (G2H) | 1 MINOR de redacción: AR-54-V4-01 |
| **V5 `26ca923`** | **AGREED** | **AGREED** (G2J) | **ninguno** |

**Proposal V5 no se modifica.** Su cabecera conserva el estado con que se publicó («Coordinator = NOT YET AGREED ON
V5», «Architect = NOT YET REVIEWED ON V5») y **no** se reescribe: los veredictos posteriores pertenecen a este
registro, no al documento revisado. Las Proposals V1..V4 se conservan intactas como registro de cada ronda.

---

## 3. ADR-0039 — propuesto

```text
ADR number           = ADR-0039
ADR path             = docs/adr/0039-custom-properties-persistencia-autoridad.md
ADR title            = Contrato de persistencia y autoridad de Custom Properties
ADR status           = propuesto
Owner ADR Acceptance = PENDING
```

Enlace: [ADR-0039](../../adr/0039-custom-properties-persistencia-autoridad.md). Indexado en
[`docs/adr/README.md`](../../adr/README.md) como `propuesto`.

- **Número.** Antes de asignarlo se censó `docs/adr/` en `origin/main` y en todas las ramas remotas vivas: `main`
  llega a 0035 (I-50); 0036 está en la rama de I-52, 0037 en la de I-53 y 0038 en la de I-49; ningún ref —ramas ni
  tags— contiene ni cita un ADR-0039. No se renumeró ningún ADR ajeno.
- **Contenido.** Congela, sin rediseñar, los dieciséis puntos del alcance acordado en Proposal V5 §15: modelo e
  identidad; regla de evolución y confrontación con ADR-0034 §3; contenedor de Proyecto; contenedor de Rack;
  aislamiento de `JsonElement?` con la cota de profundidad 16 como constante del formato; sin promoción del major del
  sobre; invariante de preservación del sobre; autoridad por `RackId`, con `Kind` e igualdad canónica; pertenencia
  indeterminada; escritura atómica; `UNKNOWN != EMPTY`; sin recuperación destructiva; compatibilidad; fronteras de
  V1; sin sintaxis de I-49; y referencias futuras solo por identidad estable.
- **Queda fuera del ADR**, como fija V5 §15: la UI, los nombres de comando, los límites numéricos de tuning (50
  entradas, 80 y 1000 caracteres), los gates y el tuning.
- **No reemplaza** a ningún ADR: aplica ADR-0006, ADR-0009, ADR-0010 y ADR-0034, se aparta de forma declarada de
  ADR-0034 §3 y acota, sin reabrirla, una frase de ADR-0035.

> **El consenso Coordinador ↔ Arquitecto NO equivale a la aceptación del ADR.** Son dos actos de dos autoridades
> distintas: **solo el Owner acepta o rechaza ADR-0039** ([`docs/adr/README.md`](../../adr/README.md)).

---

## 4. Estado del freeze

Material de G2-FREEZE según la Proposal V5 §14, y su estado en el commit que versiona este registro:

| Elemento | Estado |
|---|---|
| Coordinator y Architect AGREED sobre el **mismo** SHA | **SATISFIED** — sección 2 |
| Contrato actualizado | **SATISFIED** — [contrato](../../initiatives/I-54-propiedades-personalizadas.md) |
| ADR `propuesto` con el alcance de V5 §15 | **SATISFIED** — sección 3 |
| F-01..F-13, F-14a, F-14b y la mejora de D-11.6 en `ideas-futuras.md` | **SATISFIED** — [ideas-futuras.md](../../ideas-futuras.md), sección «I-54 — hallazgos fuera de alcance» |
| Historial pre-rebase revisado preservado | **SATISFIED** — sección 6 |
| **Aceptación explícita del ADR por el Owner**, informado de R-12, F-14a y F-14b | **PENDING** |

```text
Technical Consensus  = REACHED
Coordinator          = AGREED
Architect            = AGREED
ADR                  = PROPOSED
Owner ADR Acceptance = PENDING
Consensus Freeze     = PENDING OWNER ACCEPTANCE
Implementation       = BLOCKED
G3                   = NOT AUTHORIZED
```

**En este freeze, el único bloqueo del Owner es la aceptación explícita de ADR-0039.** Las preguntas OQ de la
Proposal V5 §16 **no** bloquean el consenso y no se plantean ahora: cada una conserva su clasificación y su gate
límite (por ejemplo, el nombre del comando antes de G7 y los números de tuning, a más tardar en G9).

---

## 5. Información para el Owner (no son preguntas)

- **R-12 — builds anteriores a I-11.** Un build anterior a I-11 que reescriba el sobre de un rack **pierde** sus
  propiedades personalizadas, porque ese build no conserva campos desconocidos. Promover el major del sobre no lo
  evitaría —esos builds no tienen guarda de versión— y dejaría ilegible todo rack con propiedades para los builds
  I-11..I-53, que hoy las conservan (Proposal V5 D-08.5). Queda como riesgo residual declarado.
- **F-14a y F-14b — residuales preexistentes del sobre exterior.** No los introduce I-54 ni los corrige:
  - **F-14a**: con UTF-16 crudo inválido en el texto del sobre, `RackEmbedStore` lanza al leer y el barrido falla
    antes de cualquier mutación.
  - **F-14b**: con un surrogate suelto escapado, el sobre se lee, pero reserializarlo lanza y ese sobre no se escribe.
    Los consumidores existentes conservan su granularidad transaccional, y `RACKEDITAR` puede quedar parcialmente
    actualizado entre vistas, como ya ocurre hoy. El ejecutor de Rack de I-54 no escribe ningún miembro en ese caso
    (D-22.10).

  Los dos quedan registrados en [ideas-futuras.md](../../ideas-futuras.md) como candidatos a una iniciativa futura de
  robustez del sobre.

---

## 6. Historial pre-rebase preservado

```text
Tag    = archive/i-54-custom-properties-pre-rebase-5d25da8
Object = 7127e9aa022765ded4fd16f9623e01fe85ef64da   (tag anotado)
Target = 5d25da89972df2468f1d03243301761f5463e6eb   (Proposal V3 pre-rebase, revisada en G2F)
Remote = origin, «* [new tag]»; ls-remote resuelve ^{} a 5d25da8
```

Sigue la convención `archive/<tema>-pre-rebase-<shortsha>`. Al ser la punta pre-rebase, conserva por ancestralidad
los demás SHAs pre-rebase revisados o publicados: CLAIM `143490d`, BOOTSTRAP `f908b2f`, G1 `195964b`, V1 `7c197af` y
V2 `36c337b`. **Un solo tag basta**, y **no se elimina al cerrar I-54**.

---

## 7. Qué NO autoriza este registro

```text
G3 / implementación          = NOT AUTHORIZED
Consensus Freeze             = PENDING OWNER ACCEPTANCE
ADR-0039                     = propuesto; no aceptado
Owner Validation / AutoCAD   = NOT EXECUTED
Merge / Integration          = NOT AUTHORIZED
Proposal V5 modification     = NOT AUTHORIZED
```

**Cero producción** en este acto: ni código, ni pruebas, ni build, ni AutoCAD. `ROADMAP.md` y `HANDOFF.md` **no se
tocan**: el esquema de estados de ROADMAP (`pendiente`, `integrada`, `descartada`) no tiene categoría de freeze, y
WORKFLOW §2 reserva la fila propia y HANDOFF a la integración y el cierre. **No se crea un plan de implementación**:
ni el contrato ni la norma lo exigen, y la Proposal V5 ya contiene los gates G3..G10 (§14) y la matriz de pruebas y
validación (§12).

---

## 8. Siguiente acto

**El Owner acepta o rechaza ADR-0039**, por su propia mano y de forma explícita, tras leerlo. Solo esa aceptación
permite una sesión corta de **G2-FREEZE COMPLETION** que:

- registra aquí la aceptación textual, como sección nueva y sin reescribir las anteriores;
- cambia el estado de ADR-0039 desde `propuesto`, tocando solo su encabezado y su bloque «Aceptación del Owner»;
- declara completo el Consensus Freeze;
- ejecuta la CI documental del nuevo SHA.

La autorización de G3 se evalúa **después**, en otra orden.

---

## 9. Verificaciones hechas antes de este registro

- `git fetch --all --prune`. Rama en `26ca923492576185b753d6dbf2a852969df2accf`, igual a su upstream (0/0), árbol
  limpio, sin stash y sin operaciones Git a medias.
- `origin/main` = `f8deb675c6d1ef0e64693b157d69c4cc170d7b24`, igual a la base de la rama: **no avanzó**, así que **no
  hubo rebase** y el SHA acordado sigue siendo el de la rama (WORKFLOW §4.2).
- Proposal V5 intacta: blob `a75444702ad354b35b67bfbbf5bb955913a02c81`, idéntico en `HEAD` y en `26ca923`.
- Paralelas:
  - I-49 en `364d6c06e44273a63a7b6f6509daf357611ea77a`: sin cambios desde G2J.
  - I-52 en `8e2ae4fb9606c246bcd5d666a8f10e35f042a901`: un commit nuevo desde `545c222`, publicado durante esta sesión
    y medido de nuevo antes del commit. Solo documentación: Proposal V4, ADR-0036 actualizado y su registro. ADR-0036
    sigue `propuesto` y con el mismo número, y mantiene las propiedades de rack de I-54 en su lista cerrada de
    portadores.
  - I-53 en `7de424e4f0f69f6e2fb18fd7c2e018479aab3fbe`: un commit nuevo desde `4e00a27`, su G4 productivo, el consumidor
    Selectivo del núcleo compartido de cabeceras: 9 archivos en `src/RackCad.Application/Systems/Selective/` y 6 en
    `tests/RackCad.Tests/`. No toca el sobre, `RackEmbedStore`, `Compose`, el re-estampado, Custom Properties ni
    `docs/adr/`.

  Ninguna cambia la numeración de ADR ni el contenido de ADR-0039.
- Los hallazgos F-01..F-13 se contrastaron con `main`: los archivos que citan son idénticos entre `BASE_SHA` y
  `f8deb67`, y siguen vivos. F-01 y F-06 ya estaban registrados en `ideas-futuras.md` y se enlazan, sin duplicarlos.

---

## 10. Inmutabilidad

**`docs/initiatives/I-54-proposal-v5.md` queda INMUTABLE.** El consenso se refiere al SHA
`26ca923492576185b753d6dbf2a852969df2accf`; modificar la Proposal después de ese SHA **invalidaría el consenso que este
registro recoge**.

```text
Cualquier cambio contractual posterior a Proposal V5
  → invalida este consenso
  → exige nueva revisión Coordinator + Architect
  → y nueva decisión del Owner si cambia materialmente lo aceptado.
```

Mientras ADR-0039 siga `propuesto`, cualquier cambio de su contenido exige que el Owner lo lea de nuevo antes de
aceptarlo.

## Trazabilidad

Las decisiones posteriores del Owner sobre I-54 se añaden a este archivo como secciones numeradas, sin reescribir
lo ya registrado.

---

## 11. Decisión del Owner — ADR-0039 ACEPTADO y Consensus Freeze COMPLETO (G2-FREEZE COMPLETION, 2026-09-13)

Se añade conforme a «Trazabilidad», sin reescribir las secciones 1 a 10. Esas secciones siguen registrando la
preparación del freeze con `Owner ADR Acceptance = PENDING`, que era el estado vigente en `d84f480`, y eso no se
borra.

```text
Initiative               = I-54 — ID24 Custom Properties Foundation
Proposal                 = V5 @ 26ca923492576185b753d6dbf2a852969df2accf
Proposal blob            = a75444702ad354b35b67bfbbf5bb955913a02c81
Coordinator              = AGREED
Architect                = AGREED
Technical Consensus      = REACHED
ADR-0039                 = ACCEPTED
Owner acceptance literal = “Acepto”
Consensus Freeze         = COMPLETE
Implementation           = BLOCKED UNTIL THIS COMPLETION COMMIT HAS GREEN CI
G3                       = NOT YET AUTHORIZED

FREEZE_PREP_SHA          = d84f480f032f6a7e5e481359aeab9fd18f691fbf   (CI 34748982744: success, 4/4 jobs)
FREEZE_COMPLETION_SHA    = 386625259922fc90ec735f739d4d5ed8e9510873   (el commit que versiona esta sección)
CI run                   = 34750208424 — event push, head_sha 386625259922fc90ec735f739d4d5ed8e9510873, success, 4/4 jobs
```

Un commit no puede contener su propio SHA: `FREEZE_COMPLETION_SHA` y su CI se reportan al Coordinador al terminar
esta sesión y se anotan aquí en la siguiente sesión que escriba en la rama. **Anotados en el primer commit de G3,
como trazabilidad** (sección 12): no cambian el contrato ni ninguna decisión registrada.

### El acto

| Campo | Valor |
|---|---|
| Decisión | **ADR-0039 = ACCEPTED** |
| Owner | **Mario Pérez**, Owner del repositorio |
| Fecha | **2026-09-13** |
| Redacción literal recibida | **“Acepto”** |
| Contexto inequívoco | ADR-0039 — Contrato de persistencia y autoridad de Custom Properties · I-54 · Proposal V5 @ `26ca923492576185b753d6dbf2a852969df2accf` |
| Contenido aceptado | ADR-0039 tal como existe en `d84f480f032f6a7e5e481359aeab9fd18f691fbf` (blob `f87f9d73067f6032a21978e3de0ab4268cca70f2`) |
| Canal | Orden de G2-FREEZE COMPLETION del Coordinador de I-54, que transmite la aceptación |

La respuesta es breve pero **inequívoca**: el Coordinador la transmite referida expresamente a ADR-0039, a I-54 y a la
Proposal V5 en ese SHA. No se registra ninguna cita más larga.

**El ADR estuvo primero en `propuesto`, y eso no se borra.** Nació en ese estado en `d84f480` y pasa a `aceptado` por
este acto; el propio ADR conserva las dos fechas. El commit que versiona esta sección solo cambia su encabezado y su
bloque «Aceptación del Owner»: desde «Contexto» hasta el final, ADR-0039 queda byte a byte igual al de `d84f480`, y
desde ahora su contenido es **inmutable** ([`docs/adr/README.md`](../../adr/README.md)). Cambiar la decisión exigirá un
ADR nuevo que lo reemplace.

**Consenso técnico ≠ aceptación.** `Coordinator = AGREED` y `Architect = AGREED` establecieron que la decisión estaba
lista; aceptarla es un acto del Owner y de nadie más.

### Secuencia completa

```text
Proposal V5 @ 26ca923492576185b753d6dbf2a852969df2accf        (CI 34747220760 success)
Coordinator = AGREED
Architect   = AGREED (G2J)
Technical Consensus = REACHED

G2-FREEZE PREP: ADR-0039 propuesto, F-01..F-14b, tag archive  -> d84f480f032f6a7e5e481359aeab9fd18f691fbf (CI 34748982744 success)
Owner explicitly accepts ADR-0039                             -> “Acepto” (2026-09-13)
G2-FREEZE COMPLETION: ADR-0039 aceptado                       -> FREEZE_COMPLETION_SHA (CI pendiente)

Consensus Freeze = COMPLETE
```

### Matriz final de G2-FREEZE

| Elemento (Proposal V5 §14) | Estado |
|---|---|
| Coordinator y Architect AGREED sobre el mismo SHA | **SATISFIED** |
| Contrato actualizado | **SATISFIED** |
| ADR con el alcance de V5 §15 | **SATISFIED** — creado `propuesto` en `d84f480`, `aceptado` por esta sección |
| F-01..F-13, F-14a, F-14b y la mejora de D-11.6 en `ideas-futuras.md` | **SATISFIED** — en `d84f480`; F-14a y F-14b verificados de nuevo, sin cambios ni duplicados |
| Historial pre-rebase preservado | **SATISFIED** — `archive/i-54-custom-properties-pre-rebase-5d25da8` → `5d25da8`, verificado en local y en `origin` |
| Aceptación explícita del ADR por el Owner | **SATISFIED** — “Acepto” |

### Qué autoriza esto, y qué NO

```text
Architecture / contract    = FROZEN   (Proposal V5 @ 26ca923 + ADR-0039 aceptado)
Consensus Freeze           = COMPLETE
Implementation             = BLOCKED hasta la CI verde del commit que versiona esta sección; después, NOT STARTED
G3                         = NOT YET AUTHORIZED; tras esa CI verde, READY FOR COORDINATOR AUTHORIZATION
Owner Validation / AutoCAD = NOT EXECUTED
Candidate / Merge          = NONE
```

Un `Consensus Freeze` completo **no es** implementación autorizada ni iniciada: la orden de G3 la emite el Coordinador
en una sesión posterior. Los gates de ejecución siguen vigentes —`requires_plugin_build`, `requires_autocad` y
`requires_owner_validation` en `true`—, y `requires_owner_decision` sigue en `true` porque quedan decisiones del Owner
con gate propio, como el nombre del comando antes de G7 (Proposal V5 §16).

### Verificaciones de esta sesión

- `git fetch --all --prune`. Rama en `d84f480f032f6a7e5e481359aeab9fd18f691fbf`, igual a su upstream (0/0), árbol
  limpio, sin stash y sin operaciones Git a medias.
- `origin/main` = `f8deb675c6d1ef0e64693b157d69c4cc170d7b24`: **no avanzó**, así que **no hubo rebase**.
- Proposal V5 intacta (blob `a75444702ad354b35b67bfbbf5bb955913a02c81`); el Discovery y el paquete, sin cambios.
- CI del freeze preparatorio: run `34748982744`, `head_sha = d84f480f032f6a7e5e481359aeab9fd18f691fbf`, `success`, con
  los cuatro jobs en verde.
- Tag `archive/i-54-custom-properties-pre-rebase-5d25da8` → `5d25da89972df2468f1d03243301761f5463e6eb` en local y en
  `origin`; no se crea otro.
- `ideas-futuras.md` sin cambios: F-14a y F-14b siguen presentes, una sola vez cada uno y con la redacción acordada.
- 0039 sigue siendo exclusivo de I-54 en todos los refs.
- Paralelas:
  - I-49 en `82aa61b20c59f4377b17f57793eac8edcfd9079f`: un commit nuevo desde `364d6c0`, solo documentación (su
    contrato e `ideas-futuras.md`), sin tocar `docs/adr/`.
  - I-52 en `8e2ae4fb9606c246bcd5d666a8f10e35f042a901` e I-53 en `7de424e4f0f69f6e2fb18fd7c2e018479aab3fbe`: sin
    cambios.
- `ROADMAP.md` y `HANDOFF.md` sin tocar: el esquema de ROADMAP no tiene estado de freeze. El Consensus Freeze completo
  vive en el contrato, en ADR-0039 y en este registro.

### Siguiente acto

Con la CI del commit que versiona esta sección en verde, **el Coordinador** puede emitir la orden de G3 en una sesión
posterior. Esta sesión no la emite ni inicia la implementación.

---

## 12. Trazabilidad posterior al freeze (primer commit de G3, 2026-09-13)

Es una sección de **trazabilidad**, no un cambio de contrato: no reabre la Proposal V5, ni ADR-0039, ni ninguna
decisión de las secciones 1 a 11.

```text
FREEZE_COMPLETION_SHA = 386625259922fc90ec735f739d4d5ed8e9510873
CI run                = 34750208424
event                 = push
head_sha              = 386625259922fc90ec735f739d4d5ed8e9510873
CI result             = success (Tests Domain + Application, Build UI, UI Tests, Build Plugin without AutoCAD)
Consensus Freeze      = COMPLETE, validado documentalmente por esa CI verde
```

- Los dos huecos que la sección 11 dejó por imposibilidad de autorreferencia quedan completados en su bloque, sin
  tocar el resto de esa sección.
- Después, el Coordinador emitió la orden de G3 con `IMPLEMENTATION AUTHORIZED FOR G3 ONLY`: documento, entrada,
  identidad estable, store puro, resultados tipados, guarda y validaciones puras, mutaciones puras e intent por id, con
  sus pruebas Core (T-STO-01..20, T-MUT-01..06 y T-MUT-09). **G4, G5, G6, G7, G8, G9 y G10 siguen NOT AUTHORIZED.** La
  evidencia de G3 viaja en el cuerpo del commit que la versiona.

---

## 13. Trazabilidad — G3 COMPLETION (G3-CLOSE, 2026-09-13)

Es una sección de **trazabilidad**, no un cambio de contrato: no reabre la Proposal V5, ni ADR-0039, ni ninguna
decisión de las secciones 1 a 12, que no se reescriben. Completa lo que anunciaba la sección 12: la evidencia de G3,
que viajaba en el cuerpo de su commit, queda también aquí, junto con la revisión del Coordinador.

```text
Initiative         = I-54 — ID24 Custom Properties Foundation
Proposal           = V5 @ 26ca923492576185b753d6dbf2a852969df2accf
Proposal blob      = a75444702ad354b35b67bfbbf5bb955913a02c81   (sin cambios)
ADR-0039           = ACCEPTED   (sin cambios)
Consensus Freeze   = COMPLETE
FREEZE_COMPLETION  = 386625259922fc90ec735f739d4d5ed8e9510873   (CI 34750208424 success)

G3 authorization   = Coordinator   (IMPLEMENTATION AUTHORIZED FOR G3 ONLY, emitida tras el freeze completo)
G3_SHA             = 57f2b4e062be6b8f83b27e0cdb1b2a42eb2a4799
CI                 = 34759032979   (event push, head_sha 57f2b4e062be6b8f83b27e0cdb1b2a42eb2a4799)
Result             = success       (4/4 jobs)
Coordinator Review = G3 ACCEPTED / COMPLETE

G3                 = COMPLETE
Implementation     = STARTED
G4+                = NOT AUTHORIZED
Owner Validation   = NOT STARTED
```

### Evidencia de G3

| Clase | Resultado |
|---|---|
| Focal (`FullyQualifiedName~RackCad.Tests.CustomProperties`) | **245/245** |
| Impacto: persistencia, Project Variables, versionado y guardas de fuente, de conformidad y de namespaces | **1032/1032** |
| Core completa, antes del commit y sobre `57f2b4e` con el árbol limpio | **6533/6533**, **0 skipped** |
| IDs de prueba | T-STO-01..20, T-MUT-01..06 y T-MUT-09: **PASS** |
| Jobs de la CI 34759032979 | Tests (Domain + Application), Build UI (WPF, valida API de Application), UI Tests (WPF controls, net8.0-windows) y Build Plugin without AutoCAD: **success** |

### Alcance

```text
Scope = Application pure + Core tests only
```

- **Application, núcleo puro**: nueve archivos nuevos en `src/RackCad.Application/CustomProperties/` y
  `src/RackCad.Application/Persistence/CustomProperties*.cs`. Cubren documento y entrada, identidad estable, store,
  resultados tipados, guarda de escritura y validaciones puras, mutaciones puras e intent por id.
- **Pruebas Core**: tres archivos nuevos en `tests/RackCad.Tests/`.
- **Documentación**: solo la traza de las secciones 11 y 12 de este registro.
- **Sin tocar**: el sobre exterior, `Compose`, la autoridad por `RackId`, Plugin, el NOD físico, UI, AutoCAD, Domain y
  las pruebas existentes.

### Precisiones aceptadas por el Coordinador

**A. DEPTH.** Rige la regla de la Proposal V5 (D-05.7, D-07.4 y T-STO-09) y de ADR-0039 (§2, §3 y §11):

```text
depth <= 16 → conforme / escribible
depth >  16 → DepthLimitExceeded
```

La frase contradictoria de la orden de ejecución de G3 fue una **errata del prompt**, no del ADR ni de la Proposal V5.
G3 aplicó la regla normativa. **ADR-0039 no se modifica** por esto.

**B. UNIFY.** La unificación **no** debía probarse en G3. Pertenece a T-AUT-14, T-MUT-10 y G5 (Proposal V5 §14), y **no
se adelantó**.

### Verificaciones de esta sesión

- `git fetch --all --prune`. Rama en `57f2b4e062be6b8f83b27e0cdb1b2a42eb2a4799`, igual a su upstream (0/0), árbol
  limpio, sin stash y sin operaciones Git a medias.
- `origin/main` = `f8deb675c6d1ef0e64693b157d69c4cc170d7b24`, igual a la base de la rama: **no avanzó**. **No hubo
  rebase**, así que la evidencia exact-SHA de G3 sigue siendo la de `57f2b4e`.
- CI 34759032979 verificada de nuevo: event push, head_sha `57f2b4e062be6b8f83b27e0cdb1b2a42eb2a4799`, conclusion
  `success` y los cuatro jobs en verde.
- La Proposal V5 (blob `a75444702ad354b35b67bfbbf5bb955913a02c81`) y ADR-0039 (blob
  `539065e645aa73105c6a0b6d71d4431f9eef19e2`) no cambian desde `386625259922fc90ec735f739d4d5ed8e9510873`.
- Paralelas:
  - I-49 en `c4880af66586c535954b0ba0bff7b64b887bddb4`: un commit nuevo desde `82aa61b`, publicado después de G3. Es su
    G5 productivo: el núcleo sintáctico en `src/RackCad.Application/Expressions/`, con sus guardas y pruebas. Las
    guardas nuevas barren Domain y `Application/Expressions`, y la lista de tokens prohibidos en Application no nombra
    nada de G3. No toca Custom Properties, el sobre ni `docs/adr/0039*`.
  - I-52 en `e998a2b714eaccf6ec7bd4d8434423c572030342` e I-53 en `f271fd578b3e583f1261d9f30e97d90a838bb7fe`: sin
    cambios desde G3.
  - 0039 sigue siendo exclusivo de I-54 en todos los refs.
- `ROADMAP.md` y `HANDOFF.md` sin tocar.

### Siguiente acto

Un commit no puede contener su propio SHA: `G3_CLOSE_SHA` y su CI se reportan al Coordinador. Con esa CI en verde,
**el Coordinador** decide si autoriza G4, en una orden posterior. Esta sesión no emite esa orden ni inicia G4.

---

## 14. Trazabilidad — G4 COMPLETION, rebase y cierre (G4-REBASE-CLOSE, 2026-09-13)

Es una sección de **trazabilidad**, no un cambio de contrato: no reabre la Proposal V5, ni ADR-0039, ni ninguna
decisión de las secciones 1 a 13, que no se reescriben. Completa lo que anunciaba la sección 13 y registra G4, su
aceptación por el Coordinador y el rebase obligatorio sobre `main`.

La sección 13 dejó pendientes `G3_CLOSE_SHA` y su CI. Se anotan aquí como trazabilidad:
`17b08f8eafbdc0b9dc99334fc79363873b9aca7d`, CI 34769492242 (push, `success`, 4/4).

```text
Initiative         = I-54 — ID24 Custom Properties Foundation
Proposal           = V5 @ 26ca923492576185b753d6dbf2a852969df2accf   (blob a75444702ad354b35b67bfbbf5bb955913a02c81, sin cambios)
ADR-0039           = ACCEPTED   (blob 539065e645aa73105c6a0b6d71d4431f9eef19e2, sin cambios)

G4 authorization   = Coordinator   (G4 AUTHORIZED; G5..G10 NOT AUTHORIZED)
G4A_SHA            = dcc404853cad549708738488966920331e884fee   (CI 34771155216: push, success, 4/4)
G4_SHA             = 78e6696e64db50ffcfbcd7e87d7dfe0a9ca78417   (CI 34772215008: push, success, 4/4)
Coordinator Review = G4 ACCEPTED / COMPLETE

Archive tag        = archive/i-54-g4-pre-rebase-78e6696 -> 78e6696e64db50ffcfbcd7e87d7dfe0a9ca78417
Rebase             = f8deb675c6d1ef0e64693b157d69c4cc170d7b24 -> 1b091bedafceb67ca57054a9eb3bf5259efff774
REBASED_G4_SHA     = 7b0f6a9ca849044acd5326f4bbcc68b6aa8fa5f0   (CI 34773339064: push, success, 4/4)

G3                 = COMPLETE
G4                 = COMPLETE
Implementation     = STARTED
G5                 = NOT AUTHORIZED
Owner Validation   = NOT STARTED
```

### Evidencia de G4 (sobre los SHAs originales)

| Clase | Resultado |
|---|---|
| G4A, focal de caracterización (T-CHR-01..04) | **103/103**, en verde sin tocar producción |
| G4B, focal `CustomPropertiesEnvelope` | **275/275** (T-CHR 103 + T-ENV, T-CPY y T-GRD 172) |
| G3 + G4 (`CustomProperties`) | **520/520**; G3 sigue en 245 |
| Impacto de persistencia y sobre | **1509/1509** |
| Core completa | **6636/6636** en G4A y **6808/6808** en G4B, **0 skipped** |
| RED de G4B | compilación sin producción: 51 errores; esqueleto temporal: 82 fallos (81 esperados y 1 defecto de la prueba, corregido) |
| Guardas en rojo | violaciones temporales no versionadas para T-GRD-01 y T-GRD-02, y una copia del restamp fuera del repositorio para T-GRD-03 |

| ID | Casos | | ID | Casos | | ID | Casos |
|---|---|---|---|---|---|---|---|
| T-CHR-01 | 28 | | T-ENV-04 | 5 | | T-ENV-11 | 2 |
| T-CHR-02 | 16 | | T-ENV-05 | 6 | | T-ENV-12 | 1 |
| T-CHR-03 | 49 | | T-ENV-06 | 3 | | T-ENV-13 | 5 |
| T-CHR-04 | 10 | | T-ENV-07 | 59 | | T-CPY-01 | 1 |
| T-ENV-01 | 26 | | T-ENV-08 | 8 | | T-GRD-01 | 10 |
| T-ENV-02 | 9 | | T-ENV-09 | 14 | | T-GRD-02 | 7 |
| T-ENV-03 | 8 | | T-ENV-10 | 3 | | T-GRD-03 | 5 |

Todas en **PASS**, sobre los SHAs originales y de nuevo sobre el tip rebasado.

**Guardas.**
- **T-GRD-01**: en `src/`, solo `RackEmbedComposer.cs` construye un `RackEmbedDocument`.
- **T-GRD-02**: el censo de `RackEmbedComposer.Compose` fuera del compositor son las 7 llamadas de D-21, por archivo,
  método y origen:
  - la propagación de variables compone con `view.Embed`;
  - seis constructores de payload reciben el sobre como parámetro;
  - ninguna pasa un `null` literal.
- **T-GRD-03**: la sobrecarga con `Guid` del restamp deserializa una vez a `embed` y serializa ese mismo `embed`, sin
  construir sobres ni usar el compositor. Convive con G-R5, que no se toca.

### Archivo previo al rebase

El tag anotado `archive/i-54-g4-pre-rebase-78e6696` apunta exactamente a `78e6696e64db50ffcfbcd7e87d7dfe0a9ca78417` y está
publicado en `origin`. Por ancestralidad preserva, con sus SHAs originales, la Proposal V5 consensuada, el freeze,
G3, G3-CLOSE, G4A y G4. Es un solo tag, y el de G2G (`archive/i-54-custom-properties-pre-rebase-5d25da8`) sigue
intacto.

### Rebase sobre `main`

`main` avanzó a `1b091be` (merge de I-53 E1) **después** del preflight de G4, así que G4 se implementó y se validó
sobre `f8deb67` sin rebase, y el rebase se hizo en esta sesión, antes de cualquier edición.
- **Commits**: 14 de 14 reaplicados, en el mismo orden y con los mismos mensajes.
- **Conflictos**: solo documentales, y todos se resolvieron conservando los dos lados. No hubo conflicto productivo, y
  `RackEmbedDocument.cs`, `RackEmbedComposer.cs`, `SchemaVersionPolicy.cs` y `RackEnvelopeRestamp.cs` no intervinieron.

  | Commit | Archivo | Resolución |
  |---|---|---|
  | bootstrap | `docs/ROADMAP.md` | la fila de I-53 y la de I-54; la de I-54, byte a byte la original |
  | G2-FREEZE prep | `docs/adr/README.md` | la fila 0037 y la fila 0039 con el párrafo de I-54; delta idéntico al original |
  | G2-FREEZE prep | `docs/ideas-futuras.md` | la sección de I-53 y la de I-54; delta idéntico al original (150 líneas) |
  | G2-FREEZE completion | `docs/adr/README.md` | la fila 0037 y el paso de 0039 a `aceptado`; delta idéntico al original |

- **Contenido**: la línea 813 de `ideas-futuras.md`, con el marcador diff3 literal que ya registraba L-11, es
  contenido previo y se conserva.
- **`git range-diff`**: 11 commits `=` y 3 `!` (bootstrap, freeze prep y freeze completion), que solo cambian en líneas
  de contexto.
- **Publicación**: `git push --force-with-lease=architecture/propiedades-personalizadas:78e6696…`, aceptada como
  `78e6696...7b0f6a9 (forced update)`.

### Mapa de SHAs

Los SHAs originales **siguen siendo la autoridad de su evidencia** y no se sustituyen en ninguna sección anterior.

| Commit | Original | Post-rebase |
|---|---|---|
| Reclamo | `c60c17fa46ef1eb2e9ab696432721d7649fe7c1d` | `4eef21d07436c0334a68616f27fb6ac54226e0f4` |
| Bootstrap | `1c2d17b3de233738d646d82811ddecd8ed673611` | `98e2fab6bd6de73608e31212b1aee3808d0e01e9` |
| G1 Discovery | `97e27ea7c50289460a2c69be0e70339c3aa674d0` | `699579c6178722b53efe39f5b0adea9901e919fa` |
| G2A Proposal V1 | `728d11f09e727adda3cf993fd39601fc18614ba6` | `653d6a32233130fc9ba47831f0e08d1da9c0d6bf` |
| G2C Proposal V2 | `2d0d6665ff286a97f25024e5fa800532cf96f624` | `bca71b9ff232e3b417db0bd676c28d7a64d5ddac` |
| G2E Proposal V3 | `ff98b9ee95ea7c4f8da0b90fe949b8ebb42157ed` | `884fcb1680cbab7efc11482165463c524973f5d8` |
| G2G Proposal V4 | `8bc991c0e1854bc9eb4013421e01f0515c2e77ca` | `92bd797853b70044990db0539c8f98f08e9841a3` |
| **G2I Proposal V5** | `26ca923492576185b753d6dbf2a852969df2accf` | `64efe8d067c820274f76c4588efe58ccc924b000` |
| G2-FREEZE prep | `d84f480f032f6a7e5e481359aeab9fd18f691fbf` | `4907fc38aeea4e038e441c7a0a8070c9b53a8726` |
| **Freeze completion** | `386625259922fc90ec735f739d4d5ed8e9510873` | `238cbf705c219d29f42eb6faa5e8d49056f87c7f` |
| **G3** | `57f2b4e062be6b8f83b27e0cdb1b2a42eb2a4799` | `92a61f14e2aac4c1fcfee14c97cbea2c4a3d73f9` |
| **G3-CLOSE** | `17b08f8eafbdc0b9dc99334fc79363873b9aca7d` | `4e75bc339f5fe0bba5a49a0a087d61213ba4590e` |
| **G4A** | `dcc404853cad549708738488966920331e884fee` | `b568bef50fc2e9db0ddfc65d882669db6dc036ef` |
| **G4** | `78e6696e64db50ffcfbcd7e87d7dfe0a9ca78417` | `7b0f6a9ca849044acd5326f4bbcc68b6aa8fa5f0` |

### Equivalencia

- **Proposal V5**: blob `a75444702ad354b35b67bfbbf5bb955913a02c81`, igual en `26ca923`, en `64efe8d` y en el tip. El
  consenso sigue anclado a `26ca923`, que el tag preserva: el rebase no crea una Proposal nueva.
- **ADR-0039**: blob `539065e645aa73105c6a0b6d71d4431f9eef19e2`, igual en `3866252`, en `238cbf7`, en `78e6696` y en el
  tip.
- **Deltas**: los de G3, G3-CLOSE, G4A y G4, cada uno contra su padre, son idénticos antes y después, salvo las líneas
  `index`. El delta completo de `src/` y `tests/` de I-54 respecto de su base (18 archivos, 5689 líneas) también es
  idéntico.
- **Sobre**: `RackEmbedDocument.CustomProperties` (`JsonElement?`, omitido si es nulo), la herencia en `Compose`,
  `WithCustomProperties`, el rechazo de colisiones y `CurrentSchemaVersion = "1.0"` están intactos.
  `RackEmbedDocument.cs`, `RackEmbedComposer.cs`, `SchemaVersionPolicy.cs`, `RackEnvelopeRestamp.cs` y `RackCloner.cs`
  no cambian entre `78e6696` y `7b0f6a9`.

Con la V5 y ADR-0039 idénticos, los deltas semánticamente iguales y las suites en verde, **no hace falta una nueva
Architect Review**.

### Revalidación sobre `REBASED_G4_SHA`

- **CI de push**: 34773339064, `head_sha` `7b0f6a9ca849044acd5326f4bbcc68b6aa8fa5f0`, `success`, con Tests (Domain +
  Application), Build UI, UI Tests y Build Plugin without AutoCAD en verde.
- **Suites locales**, sobre el árbol limpio del tip:

  | Clase | Resultado |
  |---|---|
  | G3 focal (`CustomPropertiesStoreTests` y `CustomPropertiesMutationTests`) | **245/245** |
  | G4 focal (`CustomPropertiesEnvelope`) | **275/275** |
  | G3 + G4 (`CustomProperties`) | **520/520** |
  | Impacto de persistencia y sobre | **1517/1517**: los 1509 de G4 más `DynamicHeaderBatchPersistenceTests` (4) y `SelectiveHeaderBatchPersistenceTests` (4), que trajo I-53 |
  | Core completa | **6971/6971**, **0 skipped**: 6808 de G4 más 163 de `main` |

- **Pruebas por ID**: T-CHR-01..04, T-ENV-01..13, T-CPY-01 y T-GRD-01..03 en PASS, con los mismos casos por ID que
  antes del rebase.
- **Censo real de T-GRD-02 tras el rebase**: **7**, en los mismos archivos, métodos y orígenes; `main` no lo cambió.

### Paralelas medidas en esta sesión

Solo se mide el impacto sobre el sobre, el compositor, el restamp, el censo de `Compose` y Custom Properties:
- I-49 en `71268eb09a3e04231da4e25b985e175c977f8c9e`: un commit documental nuevo (amendment A1 de su V6), sin impacto.
- I-52 en `e998a2b714eaccf6ec7bd4d8434423c572030342`: sin cambios y sin impacto.
- I-53S en `6ac42caf5cfa5c936d5bdc65bfd62cb8e04c2c4f` (`feature/cabeceras-multidestino-selectivo`, nueva, sobre
  `1b091be`): reclamo y bootstrap, solo documentación, sin impacto.
- 0039 sigue siendo exclusivo de I-54 en todos los refs.

### Siguiente acto

Un commit no puede contener su propio SHA: `G4_CLOSE_SHA` y su CI se reportan al Coordinador. Con esa CI en verde,
**el Coordinador** decide si autoriza G5, en una orden posterior. Esta sesión no emite esa orden ni inicia G5.

---

## 15. Trazabilidad — G5 COMPLETION (G5-CLOSE, 2026-09-13)

Es una sección de **trazabilidad**, no un cambio de contrato: no reabre la Proposal V5, ni ADR-0039, ni ninguna
decisión de las secciones 1 a 14, que no se reescriben. Registra G5, su aceptación por el Coordinador y las tres
precisiones con que el Coordinador la acompaña.

La sección 14 dejó pendientes `G4_CLOSE_SHA` y su CI. Se anotan aquí como trazabilidad:
`477c149fb0d57bbd263d38c87b3d29467ac27816`, CI 34773953746 (push, `success`, 4/4).

```text
Initiative         = I-54 — ID24 Custom Properties Foundation
Proposal           = V5 @ 26ca923492576185b753d6dbf2a852969df2accf   (blob a75444702ad354b35b67bfbbf5bb955913a02c81, sin cambios)
Post-rebase V5     = 64efe8d067c820274f76c4588efe58ccc924b000
ADR-0039           = ACCEPTED   (blob 539065e645aa73105c6a0b6d71d4431f9eef19e2, sin cambios)
Consensus Freeze   = COMPLETE

G5 authorization   = Coordinator   (G5 IS AUTHORIZED; G6..G10 NOT AUTHORIZED)
G5_SHA             = 3cf0d35099a6b91ced32b818fee969147f11fc77
CI                 = 34778231177   (event push, head_sha 3cf0d35099a6b91ced32b818fee969147f11fc77)
Result             = success       (4/4 jobs)
Coordinator Review = G5 ACCEPTED / COMPLETE

G3                 = COMPLETE
G4                 = COMPLETE
G5                 = COMPLETE
Implementation     = STARTED
G6+                = NOT AUTHORIZED
Owner Validation   = NOT STARTED
```

### Evidencia de G5

| Clase | Resultado |
|---|---|
| G5 focal (`CustomPropertiesAuthorityTests`, `CustomPropertiesWorkspaceTests` y `CustomPropertiesCommitTests`) | **143/143**, antes del commit y sobre `3cf0d35` con el árbol limpio |
| CustomProperties combinado (`FullyQualifiedName~RackCad.Tests.CustomProperties`) | **663/663**: G3 245, G4 275 y G5 143 |
| Impacto de persistencia, sobre y guardas (el filtro de G4) | **1660/1660**: los 1517 medidos tras el rebase más los 143 de G5 |
| Core completa, antes del commit y sobre `3cf0d35` con el árbol limpio | **7114/7114**, **0 skipped**: 6971 tras el rebase más los 143 de G5 |
| IDs de prueba | T-AUT-01..16, T-MUT-07, T-MUT-08 y T-MUT-10: **PASS** |
| RED | sin producción no compila: 36 errores únicos, todos CS0246 por los tipos de G5. Con las entradas neutralizadas temporalmente (API presente y cuerpo que lanza, nunca versionado), fallan 141 de 143; las 2 que pasan son estructurales: el orden del enum y los tipos sin UI |
| Jobs de la CI 34778231177 | Tests (Domain + Application), Build UI (WPF, valida API de Application), UI Tests (WPF controls, net8.0-windows) y Build Plugin without AutoCAD: **success** |

| ID | Casos | | ID | Casos | | ID | Casos |
|---|---|---|---|---|---|---|---|
| T-AUT-01 | 4 | | T-AUT-08 | 3 | | T-AUT-15 | 9 |
| T-AUT-02 | 3 | | T-AUT-09 | 5 | | T-AUT-16 | 21 |
| T-AUT-03 | 8 | | T-AUT-10 | 7 | | T-MUT-07 | 14 |
| T-AUT-04 | 7 | | T-AUT-11 | 3 | | T-MUT-08 | 7 |
| T-AUT-05 | 3 | | T-AUT-12 | 4 | | T-MUT-10 | 11 |
| T-AUT-06 | 3 | | T-AUT-13 | 14 | | Preflight | 3 |
| T-AUT-07 | 1 | | T-AUT-14 | 12 | | Igualdad canónica | 1 |

### Alcance

```text
Scope = Application pure + Core tests only
G6+   = NOT AUTHORIZED
```

- **Application pura**: seis archivos nuevos en `src/RackCad.Application/CustomProperties/`. Cubren:
  - la proyección plana y la selección;
  - la pertenencia por `RackId`, `IndeterminateMembership`, las xref, `MixedKind` y `UnknownKind` con `isKnownKind`
    inyectado;
  - la clasificación por colección y la igualdad canónica;
  - el orden único de ocho resultados y la unificación segura;
  - el workspace, el preflight y el commit fresco con su plan serializado completo.
- **Pruebas Core**: cuatro archivos nuevos en `tests/RackCad.Tests/`. Ningún archivo existente cambia.
- **Documentación**: ninguna en el commit de G5. La orden de G5 reservó para G5-CLOSE la corrección del índice de ADR.
- **Sin tocar**:
  - Plugin, AutoCAD, el NOD físico, comandos y UI;
  - el ejecutor de Proyecto y el ejecutor físico de Rack;
  - Domain, `RackEmbedDocument`, `RackEmbedComposer`, `RackEmbedStore` y el código de G3 y G4;
  - el restamp, `ScanEnvelopes`, el registro de handlers, Project Variables y la biblioteca;
  - las pruebas existentes.

  El censo de `Compose` sigue en 7.

### Precisiones aceptadas por el Coordinador

**A. SELECTION MISSING.** `RackCustomPropertiesAuthority.Evaluate` puede lanzar (`ArgumentException`) cuando la
selección no es de una xref y su handle no está en la proyección, porque esa es una **precondición de la API pura**. En
el commit fresco, `CustomPropertiesCommit.FreshAuthority` intercepta antes ese caso y devuelve la negativa tipada
**`SelectionMissing`**, sin plan. Esto cumple D-22.5.

**B. INDETERMINATE READ.** `IndeterminateMembership` puede conservar y exponer los miembros interpretables conocidos,
**exclusivamente** para diagnóstico y resúmenes de solo lectura.
- **No** expone una colección autoritativa del rack: `Collection` y `WriteVersion` solo existen con `Single`.
- **No** permite escritura.

**C. NORMAL CRUD SNAPSHOT.** Para el CRUD normal, la lectura fresca debe revalidar:
- el mismo rack;
- el mismo conjunto de miembros;
- la autoridad `Single`;
- el kind;
- el estado escribible;
- el intent por id y sus reglas.

No necesita comparar la forma canónica completa de cada colección mostrada si el estado fresco sigue siendo `Single`.
La comparación exacta del estado mostrado de **todos** los miembros es un requisito **específico de la unificación**
(C-5B / T-MUT-10). Así lo implementa G5:
- `CustomPropertiesCommit.ForRack` exige `Single` y compara el rack y el conjunto de miembros;
- `ForRackUnify` compara, además, la forma mostrada de cada miembro.

**Ni la Proposal V5 ni ADR-0039 se modifican** por estas precisiones.

### Corrección factual del índice de ADR

Por orden del Coordinador, G5-CLOSE corrige un hecho temporal del párrafo de I-54 en `docs/adr/README.md`.

- **Decía**: «Los números 0036, 0037 y 0038 están tomados por I-52, I-53 e I-49 en sus ramas y aún no figuran en este
  índice.»
- **Dice**: «El número 0037 ya figura en este índice, desde la integración de I-53 (E1) en `main`; 0036 y 0038 siguen
  tomados por I-52 e I-49 en sus ramas y aún no figuran en él.»

Estado verificado en esta sesión, antes de editar y otra vez antes del commit:
- `origin/main` tiene los archivos y las filas de 0035 y 0037; la fila de 0037 entró con el merge `1b091be`.
- 0036 está solo en I-52, en `91bdd385f1d3f8d3a799e282da52c134dcb8529a`, `propuesto`.
- 0038 está solo en I-49: en `71268eb09a3e04231da4e25b985e175c977f8c9e` al abrir la sesión y en
  `cae7a9f5ae10e9c86c16a29000c0933e2187873f` tras su rebase.
- 0039 sigue siendo exclusivo de I-54.

No cambian el estado, el contenido ni la numeración de ADR-0039, ni ninguna decisión.

### Verificaciones de esta sesión

- `git fetch --all --prune`. Rama en `3cf0d35099a6b91ced32b818fee969147f11fc77`, igual a su upstream y al remoto, con el
  árbol limpio, sin stash y sin operaciones Git a medias.
- `origin/main` = `1b091bedafceb67ca57054a9eb3bf5259efff774`, igual a la base de la rama: **no avanzó**. **No hubo
  rebase**, así que la evidencia exact-SHA de G5 sigue siendo la de `3cf0d35`.
- CI 34778231177 verificada: event push, head_sha `3cf0d35099a6b91ced32b818fee969147f11fc77`, conclusion `success` y
  los cuatro jobs en verde.
- La Proposal V5 (blob `a75444702ad354b35b67bfbbf5bb955913a02c81`) y ADR-0039 (blob
  `539065e645aa73105c6a0b6d71d4431f9eef19e2`) no cambian.
- Paralelas, solo sobre Custom Properties, el sobre, el censo de `Compose` y la numeración de ADR:
  - I-49: al abrir la sesión seguía en `71268eb09a3e04231da4e25b985e175c977f8c9e`, como en la sección 14. Durante la
    sesión se publicó rebasada sobre `1b091be`, en `cae7a9f5ae10e9c86c16a29000c0933e2187873f`, y se midió antes del
    commit:
    - `git range-diff` da 12 commits `=` y 4 `!` (bootstrap, ADR-0038 propuesto, su aceptación y G4), que solo cambian
      por contexto de `ROADMAP.md`, del índice de ADR e `ideas-futuras.md`;
    - el delta de `src/` y `tests/` es idéntico (mismo patch-id);
    - no toca el sobre, Custom Properties ni ADR-0039, y no añade llamadas a `Compose`.
  - I-52 en `91bdd385f1d3f8d3a799e282da52c134dcb8529a`: observado al abrir G5, un commit documental desde `e998a2b`
    (Proposal V6, ADR-0036 `propuesto` y su registro). No toca archivos de I-54 y registra el G4 de I-54 como no
    material para ella. Su Proposal V6 prevé que su espejo lleve el censo de T-GRD-02 de 7 a 8, un cruce ya previsto
    por D-21.
  - I-53S en `6ac42caf5cfa5c936d5bdc65bfd62cb8e04c2c4f`: sin cambios.
- `ROADMAP.md`, `HANDOFF.md`, el Discovery e `ideas-futuras.md` sin tocar.

### Siguiente acto

Un commit no puede contener su propio SHA: `G5_CLOSE_SHA` y su CI se reportan al Coordinador. Con esa CI en verde,
**el Coordinador** decide si autoriza G6, en una orden posterior. Esta sesión no emite esa orden ni inicia G6.
