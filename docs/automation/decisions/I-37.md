# Decisiones vinculantes del Owner — I-37 (Cantilever)

> Decisiones **normativas** emitidas por el Owner al abrir **I-37A**. Gobiernan **toda** la línea I-37, no
> solo su primera subiniciativa: una fase posterior no puede reabrirlas sin detenerse y pedir una decisión
> nueva.
>
> La autorización para registrar I-37A en `docs/ROADMAP.md` desde su propia rama es **instrucción directa
> del Owner**, con el precedente literal de I-36A. Sin ella, WORKFLOW §8 lo prohibiría.

## 1. Alcance de I-37 (paraguas)

| # | Decisión | Estado |
|---|---|---|
| 1.1 | **Los troqueles están DENTRO de I-37.** Un troquel es la rejilla de perforaciones que fija las elevaciones; sin él no hay cantilever que dibujar | vinculante |
| 1.2 | **Las placas están DENTRO de I-37, como geometría VISUAL.** Placa frontal, placa posterior, placa inferior de columna y cartabón se modelan por su contorno y su espesor | vinculante |
| 1.3 | **El cálculo resistente está FUERA.** Cargas, capacidad, flecha, pandeo y dimensionado son I-38 y no reabren [ADR-0017](../../adr/0017-validacion-cargas-diferida-ram-elements.md) | vinculante |
| 1.4 | **La fabricación está FUERA.** Soldaduras, anclas, tornillería, preparación de extremos, tolerancias, CNC y shop drawings | vinculante |
| 1.5 | **El peso queda DIFERIDO.** No entra al modelo ni al BOM en I-37A | vinculante |
| 1.6 | **La pendiente del brazo será PARAMETRIZABLE**, nunca una constante cableada. No se implementa en I-37A | vinculante para la subiniciativa del brazo |
| 1.7 | **I-37 se parte en subiniciativas.** La fila paraguas de I-37 deja de ser ejecutable y pasa a ser el índice de sus partes | vinculante |

## 2. Anatomía y vocabulario

| # | Decisión | Estado |
|---|---|---|
| 2.1 | **El separador y el arriostre son piezas DISTINTAS.** No se modelan como la misma con otro nombre, y ninguna nace en I-37A | vinculante |
| 2.2 | **La base y la columna son DEPENDIENTES**, no dos piezas que se dibujan por separado: comparten el patrón de agujeros de su conexión, y ese patrón tiene una sola autoridad | vinculante |
| 2.3 | Vocabulario obligatorio: **columna**, **base**, **placa**, **cartabón**, **troquel**. **Prohibido «ménsula»** para cualquier pieza de Cantilever: el glosario ya la asigna al conector larguero→poste, con catálogo real y FK | vinculante |

## 3. Vistas del MVP

| # | Decisión | Estado |
|---|---|---|
| 3.1 | **Frontal, lateral y planta son OBLIGATORIAS** en el MVP de Cantilever | vinculante para la subiniciativa de vistas |
| 3.2 | Ninguna vista nace en I-37A | vinculante |

## 4. Elegibilidad de secciones

| # | Decisión | Estado |
|---|---|---|
| 4.1 | La elegibilidad se decide **por combinación registrada de ids exactos**, no por una regla derivada de la familia ni de la designación | vinculante |
| 4.2 | Un **diseño ya cargado** que referencia una sección deshabilitada **sigue resolviendo y dibujando**, con advertencia y sin sustitución automática | vinculante |
| 4.3 | Una **selección nueva** ofrece únicamente secciones habilitadas y elegibles | vinculante |
| 4.4 | El primer diseño aprobado usa perfiles de familia **W**. No se generaliza a S, HSS, C ni L en I-37A | vinculante para I-37A |

## 5. Geometría aprobada del subensamble base–columna

Parámetros aprobados, **todos parametrizables**; los valores son defaults, no constantes cableadas.

### 5.1 Troqueles

| Parámetro | Valor aprobado |
|---|---|
| Diámetro | `0.75 in` |
| Offset horizontal desde cada borde | `1.50 in` |
| Pitch de la región de conexión | `2.00 in` |
| Offset vertical desde el borde de la placa posterior | `2.50 in` |
| Pitch regular de la columna | `4.00 in` |
| Troqueles de conexión por encima de la sección de la base | `3` (exactamente) |
| Pitch de la placa inferior de la columna | `2.00 in` |

### 5.2 Espesores

Cuatro parámetros **independientes**, con un default común de `0.25 in`: placa frontal de la base, placa
posterior de la base, placa inferior de la columna y cartabón. No se colapsan en una sola autoridad.

### 5.3 Reglas geométricas

- La base sale en **`+Y`** desde el plano de conexión; su sección lleva el **peralte vertical**.
- La columna es vertical (**`+Z`**); su cara de conexión define el plano `y = 0`.
- Patrón vertical de conexión: primer centro a `BaseBottomZ + 2.50`; los siguientes cada `2.00`; el último
  dentro de la sección de la base es el que no excede su envolvente superior; **exactamente tres** centros
  adicionales por encima; borde superior de la placa posterior a `último centro + 2.50`.
- Patrón horizontal: **dos filas simétricas**, con las X derivadas de la **envolvente de la columna**
  (`borde ± 1.50`), nunca de `bf`.
- Cartabón: **uno**, centrado transversalmente, triangular a **45°**, catetos iguales, cateto vertical
  `RearPlateTopZ − BaseTopZ`. Su altura **no** es `3 × pitch`: depende de dónde caiga el último troquel
  dentro de la base.
- Placa inferior de la columna: **dos filas**, una por lado, con las **mismas X** que la conexión;
  en la dirección del peralte, **pares simétricos** respecto al centro (`±p/2`, `±3p/2`, `±5p/2`, …), **sin
  troquel en el centro**, conservando solo los pares que respetan el offset desde ambos extremos.
- Región regular de la columna: el primer centro está a **`4.00`** del último de conexión —no a `2.00`, y
  sin duplicarlo—, continúa cada `4.00` y se detiene por el offset superior obligatorio. **No se recalcula
  la retícula desde el piso.**
- Una combinación cuyos agujeros gobernados por la columna **no quepan físicamente** en la placa posterior
  de la base **se rechaza**. No se inventa una regla para ensanchar la placa.

## 6. Parámetros SIN default aprobado

Estos dos valores **no tienen default**. Son entradas **obligatorias** del diseño y el resolver **rechaza**
un diseño que los omita. Ningún agente puede inventarlos:

1. **Offset desde los extremos de la placa inferior de la columna a sus troqueles.**
2. **Offset superior de la columna al último troquel regular.**

## 7. Lo que I-37A NO decide

I-37A no decide, y ninguna de sus fases puede hacerlo de contrabando: brazos y su pendiente, niveles,
estación completa, doble cara, separadores, arriostres, la línea completa, BOM, peso, persistencia de
`RackProject`, `RackSystemKind.Cantilever`, los tres registros, biblioteca, editor, preview, vistas,
AutoCAD, comandos, round-trip, materiales, cálculo, capacidad de agujeros, cálculo de placas, soldaduras,
anclas, tornillería, CNC, shop drawings, familias estructurales nuevas y bloques DWG.

Un hallazgo adyacente se registra en `docs/ideas-futuras.md`; no se implementa «de paso».

## 8. Geometría aprobada del brazo (I-37B)

Emitidas al abrir **I-37B**. Gobiernan el brazo y, donde lo digan, toda la línea.

### 8.1 Cuerpo

| # | Decisión | Estado |
|---|---|---|
| 8.1 | Un brazo puede ser **un perfil o dos canales apareados**. Los tres arreglos entran **desde ahora**: **perfil sencillo**, **canal doble encontrado**, **canal doble espalda con espalda** | vinculante |
| 8.2 | La longitud capturada es el **corte del perfil**. `NominalCutLength = GeometricLength = CutLength`, y **no** incluye el espesor de la placa de conexión, el de la tapa ni la extensión del tope | vinculante |
| 8.3 | En un canal doble, **ambos perfiles tienen exactamente el mismo corte** y la misma pendiente | vinculante |
| 8.4 | Los canales dobles están **en contacto directo**: la separación libre es **cero y no es un campo del diseño** | vinculante |
| 8.5 | Los arreglos dobles **solo admiten familia `Channel`** | vinculante |
| 8.6 | La pendiente es **`SlopeRisePer12`** y es la **única** autoridad: admite cero y positivos, y **los grados no se persisten**, se derivan | vinculante |
| 8.7 | El extremo libre **sube en los dos lados**. Un lado es una pieza: **no existe `Both`**; una estación de doble cara tendrá **dos brazos resueltos** | vinculante |

### 8.2 Conexión a la columna

| # | Decisión | Estado |
|---|---|---|
| 8.8 | El brazo **no crea retícula**: **selecciona** un conjunto **contiguo hacia arriba** de los troqueles regulares **ya resueltos** de la columna, y conserva **exactamente** sus datums | vinculante |
| 8.9 | El **pitch se observa** de los troqueles seleccionados. Ninguna constante de espaciado vive en el resolver del brazo — hoy vale 4 in porque **lo gobierna la columna** | vinculante |
| 8.10 | `VerticalPunchCount`: entero, **mínimo 2**, sin máximo, **default 2**. `LowerColumnPunchIndex`: **base cero** | vinculante |
| 8.11 | **Aumentar la cantidad de troqueles siempre extiende la placa hacia arriba.** El borde inferior queda anclado al cuerpo | vinculante |
| 8.12 | Si el perfil es **demasiado aperaltado** para las filas pedidas, **se bloquea** y se pide aumentar `VerticalPunchCount`. La placa **no** se estira en silencio | vinculante |
| 8.13 | La placa de conexión tiene el **ancho total de la columna**, centrada en X, y su **cara exterior** es donde tiene su **origen** el plano de corte del perfil | vinculante |
| 8.13.bis | Con pendiente, el corte a escuadra **NO queda a ras** de esa placa: una zona la penetra y la opuesta deja holgura. Es una **aproximación visual declarada**, informada con **ambas** magnitudes; el corte inclinado y la preparación de extremo siguen **fuera de alcance** | vinculante |

### 8.3 Placa final

| # | Decisión | Estado |
|---|---|---|
| 8.14 | Tapa y tope son **modos** de la misma placa: `None`, `Cap`, `Stop` | vinculante |
| 8.15 | El tope crece **hacia arriba** desde el mismo borde inferior, y `ExtraStopHeight` **no altera el corte del perfil** | vinculante |
| 8.16 | La placa final es **perpendicular al eje inclinado**, y su dirección de altura es la **proyección de `+Z` mundial** sobre el plano de la placa | vinculante |

### 8.4 Secciones del brazo

| # | Decisión | Estado |
|---|---|---|
| 8.17 | El **perfil sencillo es independiente de la familia**: sirve cualquier sección con variante registrada | vinculante |
| 8.18 | El perfil de producto **será HSS**, pero **I-37B no registra ningún id de producción**: el default visible se fija cuando exista editor y se registren los diseños aprobados | vinculante |
| 8.19 | **PTR no se equipara a HSS.** Si el catálogo neutral no contiene la familia o la fuente de un PTR requerido, **no se inventa la sección**, no se usa `secciones.csv` y **no se amplía I-36**: se registra como **candidato de catálogo futuro** | vinculante |

### 8.5 Parámetro SIN default aprobado

`MountingPlateVerticalEndOffset` **no tiene default**. Es entrada **obligatoria** del diseño y el resolver
**rechaza** un diseño que la omita. Se suma a los dos de I-37A, que **siguen sin default**.

## 9. Lo que I-37B NO decide

Estación completa; doble cara como sistema; lista de niveles; aplicación masiva de brazos; separadores;
arriostres; la línea; BOM; peso; persistencia; `RackSystemKind`; los tres registros; biblioteca; UI; editor;
preview; vistas; AutoCAD; comandos; bloques; cálculo; capacidad; soldaduras; tornillería; anclas; CNC; shop
drawings; familias nuevas de catálogo; y **cambios funcionales a I-36 o a I-37A**.

## 10. Geometría y reglas aprobadas de la estación (I-37C)

Emitidas al abrir **I-37C**. Gobiernan la estación y, donde lo digan, toda la línea.

### 10.1 Tipo de estación

| # | Decisión | Estado |
|---|---|---|
| 10.1 | La estación tiene **dos modos de cara**: `Single` («góndola sencilla») y `Double` («góndola doble») | vinculante |
| 10.2 | **Sencilla**: una columna, una base, una lista de niveles, **un brazo por nivel**, y todos los brazos y la base en el **mismo lado activo** (`PositiveY` o `NegativeY`) | vinculante |
| 10.3 | **Doble**: **una sola columna física**, **una sola placa inferior de columna**, **dos bases espejadas** (una por lado), **una sola lista de niveles compartida** y **dos brazos físicos por nivel** | vinculante |
| 10.4 | En doble, ambos lados comparten el **mismo índice inferior de troquel** y la **misma elevación lógica** del nivel; cada brazo puede diferir por **override de celda** | vinculante |
| 10.5 | Una góndola doble **NO se modela** como dos estaciones ni como dos subensambles columna–base completos. La base negativa se **deriva por espejo** respecto al plano central de la columna | vinculante |

### 10.2 Templates: ninguna autoridad de posición duplicada

| # | Decisión | Estado |
|---|---|---|
| 10.6 | El template de columna–base de la estación **no lleva altura**: la estación la calcula y **después** construye el `CantileverColumnBaseDesign` que consume I-37A. **Prohibida** una altura dormida en el template | vinculante |
| 10.7 | El template de brazo **no lleva `LowerColumnPunchIndex`**: la estación lo calcula y lo inyecta. El mapeo template + lado + índice → diseño vive en **un único adaptador** | vinculante |
| 10.8 | `LevelCount` **no se persiste**: es `Levels.Count`. Debe haber **al menos un nivel** | vinculante |
| 10.9 | Se persiste `FirstLevelPunchIndex` (**base cero**, obligatorio) y **no** una elevación libre del primer nivel: la elevación se **deriva** del índice | vinculante |
| 10.10 | Se persiste **un `DefaultArmTemplate`** y **únicamente overrides de celda**. `EffectiveArm = CellOverride ?? Default`. **Prohibidas** capas simultáneas global/nivel/estación/celda | vinculante |

### 10.3 Niveles y claro libre

| # | Decisión | Estado |
|---|---|---|
| 10.11 | El **claro libre** es la distancia vertical entre la **parte superior del cuerpo del brazo inferior** y la **parte inferior del cuerpo del brazo superior**, medidas **en el plano de conexión** con la columna. **No** desde ejes, centros de troquel ni bordes de placa | vinculante |
| 10.12 | `RequestedClearHeight` es finito, positivo y **global** para la estación en el MVP | vinculante |
| 10.13 | El ajuste es **obligatorio hacia arriba** a la retícula. La elevación **nunca se redondea** directamente; la salida es siempre un **índice** | vinculante |
| 10.14 | Las **placas de conexión** de dos niveles **no pueden traslaparse** | vinculante |
| 10.15 | Los **rangos de troqueles** de niveles consecutivos **no pueden compartir datums**: `CandidateLowerIndex >= PreviousLowerIndex + max(PreviousVerticalPunchCount por lado activo)` | vinculante |
| 10.16 | En doble, los dos claros se comprueban **por separado** y el candidato solo vale si **ambos** cumplen. El **lado más restrictivo gobierna** | vinculante |
| 10.17 | El cálculo usa el **brazo efectivo de cada celda** —sección, arreglo, peralte, pendiente, margen de placa, cantidad de troqueles—, nunca el default supuesto | vinculante |

### 10.4 Retícula regular y dependencia circular

| # | Decisión | Estado |
|---|---|---|
| 10.18 | La retícula regular de troqueles tiene **una autoridad única**, extraída de I-37A, que I-37A **también consume**. La fórmula `LastConnectionElevation + index × pitch` **no puede existir fuera de ella** | vinculante |
| 10.19 | La circularidad altura↔troqueles↔niveles se resuelve con una **secuencia explícita**. **Prohibidas**: altura provisional mágica, columna arbitrariamente enorme, segunda fórmula de pitch, y bucle sin contrato | vinculante |
| 10.20 | El **pase final** se **verifica** contra el layout previo. Si difiere, **falla cerrado**: una recomputación distinta no se acepta en silencio | vinculante |

### 10.5 Altura de columna

| # | Decisión | Estado |
|---|---|---|
| 10.21 | Dos modos: `Automatic` y `Manual` | vinculante |
| 10.22 | `TopClearFactor` se persiste, **default `1/3`**, finito y **≥ 1/3**. `RequestedTopClear = RequestedClearHeight × TopClearFactor` | vinculante |
| 10.23 | `LastOccupiedTop = max(cuerpo superior de los brazos del último nivel, borde superior de sus placas de conexión, elevación del último troquel utilizado)` | vinculante |
| 10.24 | `MinimumColumnHeight = max(LastOccupiedTop + RequestedTopClear, HighestUsedPunchElevation + ColumnTopPunchOffset)` | vinculante |
| 10.25 | La **tapa y el tope del extremo libre NO cuentan** para la altura de la columna: están fuera del plano de conexión | vinculante |
| 10.26 | En `Automatic`, la altura resuelta **es** la mínima, **sin redondeo comercial** (ninguno está aprobado) | vinculante |
| 10.27 | En `Manual`, `ManualColumnHeight` es obligatorio, finito, positivo y **≥ mínima**. Si es menor se **bloquea**: no se recortan niveles, no se mueven brazos, no se reduce el claro y **no se normaliza en silencio** | vinculante |

### 10.6 BOM por componentes

| # | Decisión | Estado |
|---|---|---|
| 10.28 | El BOM se deriva **únicamente de la estación resuelta**. **No** del diseño, la matriz, las vistas ni los bloques | vinculante |
| 10.29 | Una estación produce **exactamente un** componente columna–base: en sencilla con **una** base, en doble con **dos**. **Prohibido** producir dos componentes columna–base para una estación doble | vinculante |
| 10.30 | Cada **brazo físico atornillable** es un componente: uno o dos perfiles de cuerpo, una placa de conexión y cero o una placa final | vinculante |
| 10.31 | **Los troqueles no son piezas del BOM** | vinculante |
| 10.32 | Los brazos se agrupan por **receta física**, no por posición. La firma **no incluye** lado, nivel, índice de estación, coordenadas mundiales ni owner token | vinculante |
| 10.33 | Un brazo **idéntico** en `PositiveY` y en `NegativeY` es **el mismo componente** del BOM. Solo se separará por lado cuando exista una variante física derecha/izquierda, que I-37C **no introduce** | vinculante |
| 10.34 | En la receta plana: perfiles con `ProfileId = StructuralSectionId` y `Length = NominalCutLength`; placas y cartabones con categoría y descripción deterministas y **`Length = 0`**, según el precedente vigente | vinculante |
| 10.35 | **No se inventa** peso, material, costo, soldadura, tornillos ni anclas | vinculante |

### 10.7 Parámetros SIN default aprobado

I-37C **no añade ninguno**. Los **tres** heredados siguen sin default y siguen siendo entradas obligatorias
que el resolver rechaza: `ColumnBottomPlateEndOffset`, `ColumnTopPunchOffset` (I-37A) y
`MountingPlateVerticalEndOffset` (I-37B).


### 10.8 Emitidas en la revisión de I-37C

| # | Decisión | Estado |
|---|---|---|
| 10.36 | La retícula regular **ACUMULA** durante todo I-37C, para preservar exactamente el comportamiento integrado de I-37A. Pasar a `FirstElevation + index × Pitch` sería una **normalización numérica separada** y **no** forma parte de I-37C | vinculante |
| 10.37 | El **claro libre global** del MVP está **aprobado**. No es una decisión pendiente; un claro por nivel es una iniciativa posterior | vinculante |
| 10.38 | La actualización de la guarda que prohibía `BillOfMaterials` **se conserva**, siempre que sigan vigentes las guardas más estrechas que I-37C introdujo | vinculante |
| 10.39 | **No existe un máximo de candidatos** al colocar un nivel. La terminación sólo ocurre por índice encontrado, **dominio** de la retícula agotado, entrada no finita, o retícula no creciente —que es diagnóstico bloqueante—. El dominio se **deriva** de la precisión numérica, no de un límite comercial | vinculante |
| 10.40 | La conexión del brazo tiene **una sola autoridad** que valida y mide, consumida por I-37B y por el layout de I-37C. **Prohibidos** `Math.Max(2, VerticalPunchCount)` y `VerticalEndOffset ?? 0`: una entrada inválida es diagnóstico bloqueante **antes** del layout | vinculante |
| 10.41 | El **pase final** compara todas las magnitudes que decidieron el layout y la altura —incluidos `BodyBottomZ` y `BodyTopZ`— y **re-deriva** de los brazos resueltos los claros por lado, el traslape de placas, la disjunción de troqueles y la ocupación final | vinculante |
| 10.42 | Se persisten **únicamente** overrides que difieren del default, por comparación **estructural** de todos los campos editables. Un margen **ausente** no es igual a cero | vinculante |
| 10.43 | El `ProfileId` de una pieza plana representa su **receta física** —tipo, dos dimensiones, espesor y patrón de perforaciones relativo—, porque `BillOfMaterials` agrupa por `(Category, ProfileId, Length)` y `Length = 0` en las placas | vinculante |
| 10.44 | Una placa se mide **en su propio plano**, nunca con una caja alineada al mundo. La extensión de un tope se mide a lo largo del **up** del brazo | vinculante |
| 10.45 | La **firma de la estación** incluye los **brazos resueltos** por nivel y lado, además de la columna–base, los niveles, las alturas, los claros, el modo y el lado activo. El BOM **no** es autoridad de geometría | vinculante |
| 10.46 | `ActiveSides`, `OverrideFor` y `SetOverride` son **exhaustivos con fallo cerrado**. Un valor no declarado de `CantileverStationFaceMode` o de `CantileverArmSide` **no** se interpreta como `Single` ni como `NegativeY` | vinculante |

## 11. Lo que I-37C NO decide

Varias estaciones; separación longitudinal entre estaciones; la línea Cantilever; separadores; arriostres;
vistas frontal, lateral, planta e isométrica; preview; UI; editor WPF; persistencia de proyecto;
`RackSystemKind.Cantilever`; los tres registros; biblioteca; AutoCAD; comandos; Xrecords; bloques; peso;
costo; materiales; tornillería; anclas; soldaduras; cálculo estructural; capacidades; preparación de
extremos; CNC; shop drawings; familias nuevas de catálogo; un PTR nuevo; y **cambios funcionales de
comportamiento en I-36, I-37A o I-37B**.

Un hallazgo adyacente se registra en `docs/ideas-futuras.md`; no se implementa «de paso».

## 12. Geometría y reglas aprobadas del cierre del MVP (I-37D)

Emitidas al abrir **I-37D** con los veredictos `OWNER_AUTHORIZED_I_37D_FINAL_CANTILEVER_MVP` y
`OWNER_APPROVED_SEPARATOR_AND_BRACING_CONTRACTS`.

### 12.1 Línea e intervalos

| # | Decisión | Estado |
|---|---|---|
| 12.1 | Una línea tiene **al menos dos** estaciones. `StationOriginX[i] = i × ColumnCentreSpacing`, con **X** longitudinal | vinculante |
| 12.2 | `ColumnCentreSpacing` **default 48 in**, finita y positiva, medida **centro a centro** de columna | vinculante |
| 12.3 | Todas las estaciones comparten modo, lado sencillo, columna y base, cantidad de niveles, claro, retícula y **altura final** | vinculante |
| 12.4 | `IntervalCount = StationCount − 1`. Un intervalo pertenece al **par** de estaciones adyacentes: separadores y tensores **no** son de la izquierda ni de la derecha | vinculante |
| 12.5 | La altura común se resuelve en secuencia y se **verifica**: máximo de las mínimas por estación, más el mínimo del arriostramiento, y después **volver a resolver todas** comprobando que los índices no cambiaron | vinculante |
| 12.6 | `CommonMinimumHeight = max(max(StationMinimumHeight[i]), BracingMinimumHeight)`. Una altura manual menor **bloquea**: no se recortan estaciones, paneles ni niveles | vinculante |

### 12.2 Distribución vertical de paneles

| # | Decisión | Estado |
|---|---|---|
| 12.7 | `StandardBracedPanelCount = max(1, ceil((ColumnHeight − 72 in) / 60 in))`. **Reproduce las doce filas** de la tabla de producto | vinculante |
| 12.8 | `PanelCountMode` admite `Automatic` y `Manual`; la cantidad manual debe ser positiva | vinculante |
| 12.9 | `BracedPanelHeight` y `CentralEmptySpaceHeight` **default 40 in**, finitas y positivas | vinculante |
| 12.10 | `CentralEmptySpaceCount = floor((BracedPanelCount − 1) / 2)`. Los paneles se agrupan **desde abajo en bloques de máximo dos**, y el bloque incompleto queda **arriba** | vinculante |
| 12.11 | Sólo el **remanente** se reparte, por igual, a los extremos: `BottomExternalSpace = TopExternalSpace = (CommonColumnHeight − CoreHeight) / 2` | vinculante |
| 12.12 | Remanente negativo: en automática el `CoreHeight` gobierna el mínimo; en manual **bloquea**. **No** se comprimen paneles, **no** se reducen espacios centrales y **no** se cambia la cantidad en silencio | vinculante |
| 12.13 | Caso **normativo** de 264 in: 4 paneles, 1 espacio central, 32 in externos, secuencia `32·40·40·40V·40·40·32` | vinculante |
| 12.14 | `SeparatorCountPerInterval = BracedPanelCount + CentralEmptySpaceCount + 1`. Dos paneles adyacentes **comparten** su separador: se cuenta **una vez** | vinculante |

### 12.3 Placas de columna y separadores

| # | Decisión | Estado |
|---|---|---|
| 12.15 | Placa de columna de separador: **3 in × 3 in × 3/8 in**, **un** agujero centrado de **9/16 in** | vinculante |
| 12.16 | Colocada a la elevación del separador y centrada en la cara de conexión longitudinal hacia **ese** intervalo. La superficie sale de la **orientación registrada** y de la geometría resuelta: **no** se lee `d`, `bf`, `tw` ni `tf`, y **no** se supone una cara por el nombre de la sección | vinculante |
| 12.17 | Estación **extrema**: una placa por elevación. **Interior**: dos. Identidad `(stationIndex, intervalSide, separatorIndex)`, **sin duplicar** al resolver intervalos vecinos | vinculante |
| 12.18 | Las placas **son parte del componente columna–base**. **No** son componentes independientes, **no** pertenecen al separador y **no** son una categoría aparte. Columnas extremas e interiores pueden generar **recetas distintas** | vinculante |
| 12.19 | Default de producto del separador: **canal ligero de 4 in**. Si el catálogo no ofrece una correspondencia **inequívoca**, el selector queda **sin id inicial**, se exige selección del usuario y se documenta el pendiente. **No** se inventa una sección, **no** se hace matching por texto en el resolver y **no** se equipara otra familia | vinculante |
| 12.20 | `SeparatorCutLength = ConnectionPunchDistance + 2 × 1.25 in`, donde la distancia sale de los **datums de los agujeros de las dos placas** y **nunca** de centro a centro de columnas. **Prohibido** restar un ancho de columna cableado | vinculante |
| 12.21 | Cuatro troqueles de **9/16 in**: columna a **1.25 in** del borde y tensor a **4 in** de aquél, en cada extremo. `1.25 / 5.25 / L−5.25 / L−1.25`. El patrón derecho es el **espejo exacto** del izquierdo | vinculante |
| 12.22 | La coincidencia con la placa se comprueba por **datum lógico**, nunca por centros 3D de superficies distintas | vinculante |
| 12.23 | Cada separador físico es un **componente independiente** con un perfil. Sus cuatro agujeros son parte de su identidad y **no** líneas del BOM | vinculante |

### 12.4 Paneles y tensores

| # | Decisión | Estado |
|---|---|---|
| 12.24 | Cada panel lleva **dos** tensores en X: `BraceA` de abajo-izquierda a arriba-derecha y `BraceB` de abajo-derecha a arriba-izquierda, de agujero de tensor a agujero de tensor | vinculante |
| 12.25 | Los dos ocupan el **mismo plano**, pueden **solaparse visualmente**, **no** tienen unión central y **no** llevan offset. El MVP **no calcula** la interferencia entre ambos | vinculante |
| 12.26 | `CantileverBraceBodyKind` tiene `StructuralSection` y `ColdRolledRound`. **Default: cold rolled de 3/4 in** | vinculante |
| 12.27 | Tensor estructural: familias elegibles **Channel** y **Angle**, con ids exactos registrados. Agujero de **9/16 in** a **1.25 in** de cada extremo. `BraceCutLength = distancia entre datums de tensor de los separadores + 2 × 1.25 in` | vinculante |
| 12.28 | Tensor cold rolled: diámetro editable, finito y positivo, default **0.75 in**. Su eje conecta los agujeros de sus **adaptadores** y su longitud nominal es la distancia entre sus datums de paso. **No** se inventan tolerancias, roscas, tuercas ni extensiones | vinculante |
| 12.29 | Adaptador: ángulo **2 in × 2 in**, corte **2 in**, espesor **3/16 in**, un agujero **centrado** de **9/16 in** en cada cara cuadrada, y **dos cartabones**. Puede modelarse con geometría propia: **no** es obligatorio añadirlo al catálogo estructural | vinculante |
| 12.30 | Por tensor cold rolled: **2 adaptadores y 4 cartabones calibre 10**. El **calibre 10 es la autoridad**: se conserva como `GaugeNumber = 10` y **no** se convierte a decimal en silencio. Si existe una tabla de calibres en el repositorio, se **reutiliza** | vinculante |
| 12.31 | Cada tensor es un **componente**, separado por clase, sección o diámetro, corte, patrón de agujeros y —en el cold rolled— sus adaptadores y cartabones. Adaptadores y cartabones **no** son componentes separados | vinculante |

### 12.5 Matriz, BOM y políticas

| # | Decisión | Estado |
|---|---|---|
| 12.32 | La matriz es **estación × nivel × lado**, con alcances **celda**, **nivel** (ese nivel en todas las estaciones y lados activos), **estación** y **todo**. En sencilla el lado inactivo **no existe** | vinculante |
| 12.33 | Un solo `DefaultArmTemplate`; overrides **sólo** para celdas que difieren. Aplicar el default guarda `null`, aplicar el mismo valor es **no-op**, y cada override guarda **copia profunda** | vinculante |
| 12.34 | Los componentes de una línea son **cuatro**: columna–base, brazo, separador y tensor. Las columnas agrupan sólo si su receta **completa** coincide, incluidas la cantidad y las posiciones relativas de sus placas de separador | vinculante |
| 12.35 | Los brazos se agregan de **todas** las estaciones con la identidad que ADR-0026 aprobó: por receta física, sin lado ni posición | vinculante |
| 12.36 | Las políticas terminan registrando **ids exactos**. La familia sirve **sólo** para construir el conjunto de variantes durante la composición de la política, nunca para resolver después | vinculante |
| 12.37 | **No** se inventa un tamaño de HSS por omisión para el brazo: la UI puede mostrar «HSS — seleccione tamaño» y el sistema **bloquea** hasta que haya un id exacto | vinculante |

### 12.6 Persistencia, vistas, editor y dibujo

| # | Decisión | Estado |
|---|---|---|
| 12.38 | Se persiste la **intención** versionada, **nunca** el resultado resuelto. Round-trip obligatorio: `Design → serialize → deserialize → resolve → la misma firma`. Los **overrides viajan** en el documento | vinculante |
| 12.39 | Cantilever se añade al **registro vigente** con su descriptor y sus handlers. **Ningún string de kind fuera del registro**, y un kind sin handler es **error visible** | vinculante |
| 12.40 | Las tres vistas —**frontal**, **lateral**, **planta**— son **planes puros** en Application, materializados **sólo** en el Plugin. Domain y Application **no dibujan** | vinculante |
| 12.41 | La **lateral** es de **una** estación seleccionada, con `LateralStationIndex` default 0, y se deriva de la estación **resuelta y colocada**, no de su diseño | vinculante |
| 12.42 | El editor va **sobre el shell visual común** y reutiliza los controles compartidos. **Una** operación de matriz produce **una** notificación y **una** regeneración: **no** hay regeneración por celda | vinculante |
| 12.43 | La materialización **reutiliza** el Drawing vigente y **no copia** código de Selectivo, Dinámico ni Push Back. Si hay una responsabilidad realmente compartida se **extrae**, con caracterización previa | vinculante |
| 12.44 | El **gate de I-37D es el veredicto manual del Owner en AutoCAD 2025**, con DLL, bundle, `NETLOAD` y checklist. CI verde es **necesario y no suficiente**. Sin ese veredicto **no se integra** y **I-37 no se cierra** | vinculante |

### 12.7 Emitidas al autorizar la implementación completa (2026-07-30)

Veredictos: `OWNER_APPROVED_I_37D_SEAM_AUDIT`, `OWNER_DECIDED_CANTILEVER_IDENTITY_PER_LINE`,
`OWNER_DECIDED_CANTILEVER_VIEW_SCOPE`, `OWNER_DECIDED_RACK_SYSTEM_KIND_CANTILEVER`,
`OWNER_DECIDED_SEPARATOR_DEFAULT_AISC_C_C4X4_5`, `OWNER_DECIDED_CANTILEVER_FACE_MODE_ENUM_PERSISTENCE`,
`OWNER_AUTHORIZED_I_37D_FULL_IMPLEMENTATION`.

Cierran las preguntas abiertas de la auditoría de costuras. Ninguna es reinterpretable durante la
implementación.

| # | Decisión | Estado |
|---|---|---|
| 12.45 | La **auditoría de costuras** de I-37D queda **aprobada** como base de la implementación. Sus hallazgos son el mapa; sus preguntas abiertas quedan cerradas por las decisiones de esta sección | vinculante |
| 12.46 | **Un GUID por LÍNEA**, en `CantileverLineDesign.Id`. La identidad de una estación se **deriva** de `LineId + StationIndex` y **no** se persiste aparte. Los bloques de las tres vistas **comparten** el `Id`. Duplicar **acuña un solo** GUID común nuevo | vinculante |
| 12.47 | El agregado persistido es la **línea**: `CantileverLineDesign` / `CantileverLineDocument` / `CantileverLineAssembly`, con la ranura `public CantileverLineDocument Cantilever { get; set; }` en el wrapper. **No** se registra `CantileverStationDocument` como payload superior | vinculante |
| 12.48 | **Frontal** y **planta** son de la línea completa, con `Section = -1`; la **lateral** es de una estación, con `Section = StationIndex`. La creación inicial inserta **exactamente** una frontal, una planta y una lateral del índice 0. **No** se insertan N laterales automáticamente | vinculante |
| 12.49 | `LateralStationIndex` vale **0** por omisión, pertenece al estado de **vista/editor**, se estampa en `RackEmbedDocument.Section`, **no** vive en `CantileverLineDesign`, y **no** afecta el BOM ni la firma física | vinculante |
| 12.50 | `RackSystemKind.Cantilever = 6`, **al final** del enum. Nombres **congelados desde la primera persistencia**: JSON `"Cantilever"`, embed kind `"cantilever"`, comando `RACKCANTILEVER`, etiqueta de biblioteca `Cantilever`, sustantivo de validación `la línea Cantilever`, etiqueta de BOM del Plugin `Cantilever` | vinculante |
| 12.51 | El separador por omisión es **exactamente** `AISC-C-C4X4_5`. **No** se busca por designación durante la resolución | vinculante |
| 12.52 | `CantileverStationFaceMode` se persiste como **enum directo** con la infraestructura JSON vigente. **No** se crea un convertidor especial. Un nombre desconocido **falla cerrado**; un valor numérico no declarado es **diagnóstico bloqueante** | vinculante |
| 12.53 | El calibre de los cartabones del adaptador se persiste como **identidad `10`** y se describe `CAL_10`. **No** se inventa un espesor decimal: el repositorio no tiene tabla de calibres | vinculante |
| 12.54 | `CantileverSectionResolver.Noun` es un `switch` **exhaustivo** con `default` que lanza. Los demás hallazgos adyacentes de la auditoría **no** se corrigen en I-37D | vinculante |

## 13. Lo que I-37D NO decide

Cálculo resistente; cargas; capacidad; I-38; peso; costo; optimización; soldaduras; tornillos y tuercas;
anclas; roscas; tolerancias; preparación de extremos; CNC; shop drawings; la **interferencia física en el
cruce de tensores**; y cualquier **catálogo nuevo sin procedencia**.

Siguen fuera **incluso al cerrar I-37**. Un hallazgo adyacente se registra en `docs/ideas-futuras.md`; no se
implementa «de paso».

## 13. Ronda 2 de I-37D - el rechazo de la validacion manual y lo que decidio

**Veredicto registrado:** `OWNER_REJECTED_I_37D_MANUAL_VALIDATION_ROUND_1`.

Seis motivos, todos de arquitectura visual y de flujo. Las decisiones que la ronda 2 tomo para atenderlos,
vinculantes desde aqui:

| # | Decision | Fuerza |
|---|---|---|
| 13.1 | La ventana principal de Cantilever edita **solo** `CantileverLineDesign`: linea, topologia compartida y distribucion del arriostramiento. Ningun parametro interno de componente vive en ella | vinculante |
| 13.2 | Cada componente -columna y base, brazo, separador, tensor- se edita en su **propia ventana**, sobre un shell comun, con parametros, preview, diagnosticos, receta y acciones propios | vinculante |
| 13.3 | La eleccion de seccion se hace con un **selector buscable** con filtro de familia. Un `ComboBox` de catalogo no es aceptable | vinculante |
| 13.4 | **Buscar no es resolver.** El texto encuentra una fila; la fila devuelve un id EXACTO. La busqueda parcial nunca se usa como resolucion | vinculante |
| 13.5 | `BaseFollowsColumn` es **intencion persistida**, no una comparacion de ids. Una seccion no elegible como base **no** normaliza y **se diagnostica** | vinculante |
| 13.6 | La representacion dibuja **todos** los agujeros resueltos: circulo real cuando la proyeccion lo permite, traza cuando el agujero esta de canto. Ninguno se omite por pequeno | vinculante |
| 13.7 | Un componente insertado **solo** es una pieza suelta: identidad propia, sin payload, sin kind de linea, sin round-trip. **No editable**, y declarado como tal | vinculante |
| 13.8 | Una operacion de matriz es **una** escritura agregada, **una** notificacion y **una** regeneracion. Nunca una por celda | vinculante |
| 13.9 | Cancelar una subventana **no** muta el diseno original: cada configurador edita una copia | vinculante |
| 13.10 | Los diagnosticos de un componente se muestran **en su ventana**, no al volver a la principal | vinculante |

ADR-0027 y ADR-0028 estaban **propuestos** cuando se escribio esta seccion. Quedaron **aceptados** el
2026-08-03, tres rondas despues, con la validacion manual del Owner.

## 14. Ronda 4 de I-37D — el adaptador físico y el editor avanzado de paneles

El dueño revocó la aproximación con la que se situaba el agujero de varilla del adaptador y autorizó el
editor avanzado de paneles que la ronda 3 había dejado fuera de alcance. Las decisiones que emitió, todas
vinculantes desde aquí:

| # | Decisión | Fuerza |
|---|---|---|
| 14.1 | **El adaptador físico manda sobre el tensor.** Queda REVOCADA `RodHoleAxialOffset = CutLength / 2`: su justificación —«medio corte, porque las dos caras son perpendiculares»— es la que no se sostiene, porque si son perpendiculares la separación tiene componente en los dos ejes | vinculante |
| 14.2 | Los **dos agujeros están centrados cada uno en SU ala**, en el **plano medio real** de esa ala. `ΔY` **no se fuerza a cero**: la separación tiene sus tres componentes derivadas del marco | vinculante |
| 14.3 | El **centro del agujero de varilla es el datum físico** del extremo del tensor. La longitud nominal es la distancia entre los dos | vinculante |
| 14.4 | El adaptador se modela como **prisma real** de `AISC-L-L2X2X3_16` con `PrismaticSectionInstance`, corte de 2 in, marco físico y mano, **proyectado por la tubería estructural común**. Prohibido construir otra L plana a mano | vinculante |
| 14.5 | Si cambia la longitud nominal, **cambia la línea del BOM** y se actualizan los goldens con la causa física. **No se conserva el BOM anterior por compatibilidad artificial** | vinculante |
| 14.6 | **No se deforma ninguna vista de la línea** para que el adaptador parezca una L. Frontal, lateral y planta siguen siendo **proyecciones físicas** | vinculante |
| 14.7 | El configurador de tensor gana una vista propia, **«Sección del adaptador»**, que mira a lo largo del eje longitudinal de corte y muestra la sección real: dos alas, espesor, filete de raíz, radios pequeños en las puntas libres, talón exterior vivo y los centros de agujero visibles en esa proyección. Debe consumir la **misma** `StructuralSectionGeometry` que el prisma — ni otra L manual, ni otra fórmula de radios | vinculante · **IMPLEMENTADA** en `dd9e4a5` |
| 14.8 | La inserción independiente del tensor **puede** incluir esa vista como detalle adicional. Las vistas de la línea **no cambian de orientación** | vinculante |
| 14.9 | La secuencia vertical de paneles admite **dos modos**, `Automatic` y `Advanced`, y **los dos producen la misma lista efectiva**, que es la **única entrada** del resolver posterior | vinculante |
| 14.10 | **Un espacio vacío es un tramo explícito** con `BracingMode = None`. Un hueco implícito se rechaza | vinculante |
| 14.11 | `SeparatorElevations = Distinct(inicios + fines)`. **No se duplican separadores en fronteras compartidas** | vinculante |
| 14.12 | `BracedPanels = tramos CrossBraced` y `BraceCount = 2 × BracedPanels`. **Ningún tensor en un tramo `None`** | vinculante |
| 14.13 | En modo automático se **conserva la regla estándar** de 12.x/D4 sin cambio de producto alguno | vinculante |
| 14.14 | Se persisten **modo, segmentos, orden y bracing mode**. **No** se persisten separadores derivados, tensores derivados ni alturas duplicadas | vinculante |
| 14.15 | `Automatic → Advanced` **materializa** la lista automática vigente como segmentos editables. `Advanced → Automatic` **advierte** que la lista manual deja de mandar antes de devolver la autoridad a la regla | vinculante |
| 14.16 | Los errores del editor avanzado se muestran **en la misma ventana** | vinculante |

### Lo que la ronda 4 NO decidió, y qué pasó después

Cuando se escribió esta sección, la ronda 4 no aceptaba los ADR ni cerraba nada: la validación manual seguía
siendo el gate y seguía pendiente. **Se ejecutó el 2026-08-03 y quedó APROBADA**
(`OWNER_APPROVED_I37D_MANUAL_VALIDATION`, sin defectos bloqueantes), así que:

- **ADR-0027 y ADR-0028 quedaron `aceptados`**, auditados punto por punto antes de firmarlos.
- **I-37D queda validada** e integrable.
- **Sigue sin resolverse** la ambigüedad preexistente de `CantileverPlatePlan.NearOffset`, declarada en la
  ronda 3. No es de I-37D: unificarla toca cuatro familias de placa y es un cambio de contrato propio.

## 15. Aprobación de la validación manual de I-37D

| Campo | Valor |
|---|---|
| Token | **`OWNER_APPROVED_I37D_MANUAL_VALIDATION`** |
| Fecha | 2026-08-03 |
| Veredicto | **APROBADA** — todo funciona correctamente, sin defectos bloqueantes observados |
| `CODE_SHA` | `dd9e4a5` |
| `VALIDATED_BUILD_SHA` | `a594eb5` |
| DLL Debug SHA-256 | `F237CC7951A398751C369FB64A0A6FF541F80E37E39C4375905D2AE98985B6E1` |
| Evidencia | [paquete manual](../evidence/I-37D-autocad-validation-advanced-panels-and-adapter.md) |

Con este veredicto se cierra el gate que I-37D arrastraba desde su apertura: **el suyo nunca se resolvió
sobre el código**, y por eso cuatro rondas de CI verde no bastaron para integrarla.
