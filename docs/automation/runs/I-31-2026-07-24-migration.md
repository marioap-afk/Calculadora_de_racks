# I-31 — Corrida de migración Selectiva al shell visual (2026-07-24)

Rama `refactor/selective-visual-shell`; Claim-Id `ab7853c9-0150-4db9-a331-92571fa6b6ab`.
Worktree `~/.codex/worktrees/refactor-selective-visual-shell`. Base `origin/main = 40a2c8e`
(sin rebase: `origin/main` no avanzó durante la corrida).

## Preflight (todos verdes)

`main == origin/main == 40a2c8e`; árbol principal limpio; sin merge/rebase/cherry-pick/bisect; CI
verde del SHA exacto de `main` (run `30101393426`, success); rama y worktree de I-30 eliminados; I-31
y `refactor/selective-visual-shell` libres antes del reclamo; ninguna iniciativa activa sobre
`RackSelectiveWindow`; I-23 e I-25 sin rama remota (inactivas); `feature/push-back` en su HEAD remoto
`b2d9e9d`, worktree limpio, local == remoto.

## Commits

1. `6cdaea7` — reclamo (commit vacío; Initiative/Branch/Claim-Id + trailer Codex; primer push `-u` aceptado como rama nueva).
2. `fd4969e` — docs: contrato, estado e índice (inventario comparativo, selección confirmada sin multiselección).
3. `5b7c0b4` — `RackSelectiveWindow.xaml` compuesto sobre `RackEditorVisualShell` (solo XAML; `.cs` intacto).
4. `67a8621` — `SelectiveShellMigrationTests` (19 pruebas) + `EditorWindowTestSupport` (locator por slots del shell).
5. (este) — cierre documental: corrección de conteo 45 `x:Name`, estado a `validating`, esta nota de corrida.

## Qué cambió

`RackSelectiveWindow` deja su composición exterior propia (grid 2×3, panel de 342 px con scroll
exterior, disposición independiente de matriz/preview/status, barra inferior propia) y se compone sobre
el shell por slots `SidePanelContent`/`MatrixContent`/`PreviewContent`/`StatusContent` + categorías
`Leading`/`Secondary`/`Primary`/`Trailing`. Aplica `EditorShellWindowStyle` (tamaño 1280×720, mín.
1120×672, fondo y tipografía por tokens `Shell*`). Superficies por `ShellSurfaceBrush`/
`ShellBorderBrush`/`ShellPanelPadding`, preview por `ShellPreviewBackgroundBrush` (mismo hex; sin
duplicar paleta). Canvas y painter del preview alojados tal cual (no se adopta el control
`PreviewCanvas` de I-14, sin prueba de equivalencia). **`.cs` byte-idéntico a `origin/main`.**

Preservado (verificado): 45 `x:Name` (idénticos + `Shell`), 31 handlers (idénticos), todos los
Content/ToolTip/Text (diff vacío), selección de UNA celda + alcance Celda/Nivel/Frente/Todas (main **no**
tiene multiselección; **no** se agrega), selector y matrices por fondo, editor de celda, cabeceras/
peraltes por poste, seguridad, previews frontal/lateral, inserción frontal/lateral/planta, actualización
en sitio, BOM, biblioteca, GUID/nombre/round-trip, metadata I-11, estados habilitado/deshabilitado con
motivo + `ToolTipService.ShowOnDisabled`. Orden visual y funcional de acciones conservado.

## Pruebas y builds

- `RackCad.Tests` **1016/1016** (Debug) · `RackCad.UI.Tests` **237/237** (Debug; 218 + 19 nuevas).
- Builds Debug: `RackCad.UI` 0 errores/0 advertencias; `RackCad.Plugin` 0 errores/2 `MSB3277`
  (referencias AutoCAD, conocidas); `RackCad.sln` 0 errores/2 `MSB3277`.
- Filtros dirigidos (todos descubren ≥1 y pasan): Selective 350 (core 319 + ui 31), Dynamic 197
  (174+23), Shell 58 (ui), EditorState 28 (24+4), Persistence 112 (core), Handler 2 (core), goldens
  `Equivalence` 11 (core, incluye los 7 `SelectiveSafetyEquivalence…ResolvedOutputIsFrozen`), Validator
  33 (core, I-19). Ningún filtro vacío; 0 fallos.

## Alcance / integridad

Diff confinado a UI (`RackSelectiveWindow.xaml`), pruebas (`SelectiveShellMigrationTests.cs`,
`EditorWindowTestSupport.cs`) y documentación de I-31. Sin cambios en `src/RackCad.UI/Shell/**`,
`Themes/**`, Plugin, Application, Domain, persistencia, catálogos ni comandos; sin ramas por sistema ni
`RackSystemKind` en el shell; sin duplicación de paleta o proyección; `RackSelectiveWindow.xaml.cs`
intacto. `feature/push-back` **no tocada** (`b2d9e9d`, leída solo con `git show`). Sin dependencias
NuGet nuevas.

## DLL Debug para el Owner

`…/refactor-selective-visual-shell/src/RackCad.Plugin/bin/Debug/net8.0-windows/RackCad.Plugin.dll`
(construido desde el HEAD publicado de la rama; el commit de cierre es documental y no cambia el
binario respecto del código validado por las pruebas).

## Validación del Owner (2026-07-24)

El Owner **validó en AutoCAD 2025 los 12 puntos** del editor Selectivo migrado y los **aprobó sin
observaciones** sobre el DLL Debug del SHA `b638653b10bdba5cd5c1d9f814f196c177f18c3e`
(`AssemblyInformationalVersion = 1.0.0+b638653b10bdba5cd5c1d9f814f196c177f18c3e`; CI del candidato run
`30108459424` **success**). Gates `autocad`, `owner-validation` y `plugin_build` **resueltos**.
`origin/main` no avanzó desde `40a2c8e` → **sin rebase final**; la aprobación de `b638653` es vigente y
el commit documental de registro de aprobación no cambia `src/**`, `tests/**` ni el binario. Evidencia:
[`docs/automation/evidence/I-31-autocad-validation.md`](../evidence/I-31-autocad-validation.md).

## Handoff

Iniciativa en `integration-ready`. Sesión de integración: `main` fast-forward desde `origin/main` →
`git merge --no-ff refactor/selective-visual-shell` → CI del merge → cierre documental (HANDOFF §5/§8-12
+ ROADMAP fila I-31) → limpieza segura de rama/worktree. Después, **en su propio chat/worktree**,
**rebasar `feature/push-back` sobre el nuevo `origin/main` y reanudar I-18** (Push Back al shell); ese
rebase **no** se ejecuta dentro de I-31. `feature/push-back` permanece intacta (`b2d9e9d`).
