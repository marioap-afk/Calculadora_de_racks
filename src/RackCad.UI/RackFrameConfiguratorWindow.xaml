<Window x:Class="RackCad.UI.RackFrameConfiguratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:RackCad.UI"
        Title="RACKCABECERA"
        Width="1320"
        Height="820"
        MinWidth="1160"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="#EEF2F6"
        FontFamily="Segoe UI">
    <Grid>
        <Grid.Resources>
            <Style x:Key="PanelBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#D8DEE6" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="14" />
            </Style>

            <Style x:Key="HiddenDetailPanelStyle" TargetType="Border">
                <Setter Property="Visibility" Value="Collapsed" />
                <Setter Property="Margin" Value="0,12,0,0" />
            </Style>

            <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="#1F2933" />
            </Style>

            <Style x:Key="MutedTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#617080" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="PropertyLabelStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#617080" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="0,0,12,10" />
            </Style>

            <Style x:Key="PropertyValueTextBoxStyle" TargetType="TextBox">
                <Setter Property="BorderBrush" Value="#C9D2DD" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="7,4" />
                <Setter Property="Margin" Value="0,0,0,10" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="PropertyComboBoxStyle" TargetType="ComboBox">
                <Setter Property="BorderBrush" Value="#C9D2DD" />
                <Setter Property="Padding" Value="4,2" />
                <Setter Property="Margin" Value="0,0,0,10" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="CompactButtonStyle" TargetType="Button">
                <Setter Property="Padding" Value="10,6" />
                <Setter Property="Margin" Value="0,0,8,8" />
                <Setter Property="MinWidth" Value="88" />
            </Style>

            <Style x:Key="CellTextBoxStyle" TargetType="TextBox">
                <Setter Property="BorderBrush" Value="#C9D2DD" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="CellComboBoxStyle" TargetType="ComboBox">
                <Setter Property="BorderBrush" Value="#C9D2DD" />
                <Setter Property="Padding" Value="4,2" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F2933" Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Configurador de cabecera"
                               Foreground="White"
                               FontSize="22"
                               FontWeight="SemiBold" />
                    <TextBlock Text="Partir del estandar y modificar excepciones"
                               Foreground="#C7D0DA"
                               FontSize="13"
                               Margin="0,4,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding StandardBaselineId}"
                               Foreground="White"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Right" />
                    <TextBlock Text="{Binding StandardBaselineVersion, StringFormat=Version {0}}"
                               Foreground="#C7D0DA"
                               FontSize="12"
                               HorizontalAlignment="Right"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer x:Name="MainScrollViewer"
                      Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
        <Grid x:Name="MainContentGrid"
              Margin="16"
              MinHeight="680"
              Height="{Binding ViewportHeight, ElementName=MainScrollViewer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftColumn" Width="245" MinWidth="220" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition x:Name="CenterColumn" Width="*" MinWidth="600" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition x:Name="PreviewColumn" Width="318" MinWidth="280" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition x:Name="LeftModelRow" Height="205" MinHeight="120" />
                        <RowDefinition Height="8" />
                        <RowDefinition x:Name="LeftValidationRow" Height="122" MinHeight="95" />
                        <RowDefinition Height="8" />
                        <RowDefinition x:Name="LeftExceptionsRow" Height="*" MinHeight="140" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="Modelo" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock Text="Selecciona una parte para editar sus propiedades."
                                   Style="{StaticResource MutedTextStyle}"
                                   FontSize="12"
                                   Margin="0,4,0,12" />
                    </StackPanel>

                    <TreeView x:Name="ModelTree"
                              Grid.Row="1"
                              ItemsSource="{Binding NavigationItems}"
                              SelectedItemChanged="ModelTree_SelectedItemChanged"
                              BorderBrush="#D8DEE6"
                              BorderThickness="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <TreeView.Resources>
                            <HierarchicalDataTemplate DataType="{x:Type ui:ConfiguratorNavigationItem}"
                                                      ItemsSource="{Binding Children}">
                                <Border Padding="8,7">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#1F2933" />
                                        <TextBlock Text="{Binding Subtitle}" Foreground="#617080" FontSize="11" Margin="0,2,0,0" />
                                    </StackPanel>
                                </Border>
                            </HierarchicalDataTemplate>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded}" />
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>

                    <GridSplitter Grid.Row="2"
                                  Height="8"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  Background="#D8DEE6"
                                  ShowsPreview="True"
                                  ResizeBehavior="PreviousAndNext" />

                    <Border Grid.Row="3"
                            Background="{Binding HeightValidationBackground}"
                            BorderBrush="{Binding HeightValidationBrush}"
                            BorderThickness="1"
                            Padding="12">
                        <StackPanel>
                            <TextBlock Text="Validacion"
                                       FontWeight="SemiBold"
                                       Foreground="{Binding HeightValidationBrush}" />
                            <TextBlock Text="{Binding HeightValidationMessage}"
                                       Foreground="#415161"
                                       TextWrapping="Wrap"
                                       FontSize="12"
                                       Margin="0,5,0,0" />
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Objetivo" Foreground="#617080" FontSize="11" />
                                    <TextBlock Text="{Binding TargetHeightText}" FontWeight="SemiBold" />
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Configurada" Foreground="#617080" FontSize="11" />
                                    <TextBlock Text="{Binding ConfiguredHeightText}" FontWeight="SemiBold" Foreground="{Binding HeightValidationBrush}" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <GridSplitter Grid.Row="4"
                                  Height="8"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  Background="#D8DEE6"
                                  ShowsPreview="True"
                                  ResizeBehavior="PreviousAndNext" />

                    <Grid Grid.Row="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <DockPanel LastChildFill="True">
                            <TextBlock Text="Excepciones" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock DockPanel.Dock="Right"
                                       Text="{Binding Exceptions.Count}"
                                       Foreground="#617080"
                                       FontWeight="SemiBold" />
                        </DockPanel>
                        <TextBlock Grid.Row="1"
                                   Text="{Binding ExceptionSummary}"
                                   Style="{StaticResource MutedTextStyle}"
                                   FontSize="12"
                                   Margin="0,4,0,8" />

                        <ListBox Grid.Row="2"
                                 ItemsSource="{Binding ExceptionGroups}"
                                 BorderBrush="#D8DEE6"
                                 BorderThickness="1"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E3E8EF"
                                            BorderThickness="0,0,0,1"
                                            Padding="8">
                                        <StackPanel>
                                            <DockPanel LastChildFill="True">
                                                <TextBlock Text="{Binding TargetId, Mode=OneWay}"
                                                           FontWeight="SemiBold"
                                                           Foreground="#1F2933" />
                                                <TextBlock DockPanel.Dock="Right"
                                                           Text="{Binding CountText, Mode=OneWay}"
                                                           Foreground="#617080"
                                                           FontSize="10" />
                                            </DockPanel>
                                            <ItemsControl ItemsSource="{Binding Changes}" Margin="0,5,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock FontSize="11"
                                                                   Foreground="#617080"
                                                                   TextWrapping="Wrap"
                                                                   Margin="0,2,0,0">
                                                            <Run Text="- " />
                                                            <Run Text="{Binding FieldName, Mode=OneWay}" />
                                                            <Run Text=": " />
                                                            <Run Text="{Binding StandardValue, Mode=OneWay}" />
                                                            <Run Text=" -> " />
                                                            <Run Text="{Binding OverrideValue, Mode=OneWay}" />
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="8"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="#D8DEE6"
                          ShowsPreview="True"
                          ResizeBehavior="PreviousAndNext" />

            <Grid x:Name="CenterPanelGrid" Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="CenterPropertiesRow" Height="150" MinHeight="115" />
                    <RowDefinition Height="8" />
                    <RowDefinition x:Name="CenterQuickRow" Height="90" MinHeight="72" />
                    <RowDefinition Height="8" />
                    <RowDefinition x:Name="CenterBulkRow" Height="80" MinHeight="65" />
                    <RowDefinition Height="8" />
                    <RowDefinition x:Name="CenterTablesRow" Height="*" MinHeight="368" />
                </Grid.RowDefinitions>

                <Border Style="{StaticResource PanelBorderStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <DockPanel LastChildFill="True">
                            <StackPanel>
                                <TextBlock Text="{Binding ActiveDetailTitle}" Style="{StaticResource SectionTitleStyle}" />
                                <TextBlock Text="{Binding ActiveDetailSubtitle}"
                                           Style="{StaticResource MutedTextStyle}"
                                           FontSize="12"
                                           Margin="0,3,0,0" />
                            </StackPanel>
                            <TextBlock DockPanel.Dock="Right"
                                       Text="{Binding SelectedSegmentSummary}"
                                       Foreground="#617080"
                                       FontSize="12"
                                       VerticalAlignment="Top" />
                        </DockPanel>

                        <Grid>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Header">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Plantilla base" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding TemplateSummary, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Altura objetivo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding HeightText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Fondo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding DepthText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Unidades" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding Units, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="LeftPost">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Perfil" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPostCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Descripcion" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPostDescription, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Refuerzo" Style="{StaticResource PropertyLabelStyle}" />
                                    <CheckBox Grid.Row="1" Grid.Column="1" Content="Poste reforzado" IsChecked="{Binding LeftPostHasReinforcement, Mode=TwoWay}" Margin="0,3,0,10" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Tipo refuerzo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPostReinforcementCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Placa asociada" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPlateCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="RightPost">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Perfil" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPostCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Descripcion" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPostDescription, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Refuerzo" Style="{StaticResource PropertyLabelStyle}" />
                                    <CheckBox Grid.Row="1" Grid.Column="1" Content="Poste reforzado" IsChecked="{Binding RightPostHasReinforcement, Mode=TwoWay}" Margin="0,3,0,10" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Tipo refuerzo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPostReinforcementCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Placa asociada" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPlateCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Segments">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Claro calculado" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.ClearHeightText, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Arreglo de panel" Style="{StaticResource PropertyLabelStyle}" />
                                    <ComboBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyComboBoxStyle}" ItemsSource="{Binding PatternOptions}" SelectedItem="{Binding SelectedBracingSegment.Pattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                    <TextBlock Grid.Row="0" Grid.Column="6" Text="Cara de montaje" Style="{StaticResource PropertyLabelStyle}" />
                                    <ComboBox Grid.Row="0" Grid.Column="7" Style="{StaticResource PropertyComboBoxStyle}" ItemsSource="{Binding SideOptions}" SelectedItem="{Binding SelectedBracingSegment.SideMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Perfil" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.BraceProfileId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Punto inicial" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.StartConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="6" Text="Punto final" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="7" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.EndConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Horizontal inferior" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.LowerHorizontalId, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />

                                    <TextBlock Grid.Row="2" Grid.Column="3" Text="Horizontal superior" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.UpperHorizontalId, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />

                                    <TextBlock Grid.Row="2" Grid.Column="6" Text="Direccion diagonal" Style="{StaticResource PropertyLabelStyle}" />
                                    <ComboBox Grid.Row="2" Grid.Column="7" Style="{StaticResource PropertyComboBoxStyle}" ItemsSource="{Binding DirectionOptions}" SelectedItem="{Binding SelectedBracingSegment.DiagonalDirection, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Reinforcements">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Izquierdo" Style="{StaticResource PropertyLabelStyle}" />
                                    <CheckBox Grid.Row="0" Grid.Column="1" Content="Con refuerzo" IsChecked="{Binding LeftPostHasReinforcement, Mode=TwoWay}" Margin="0,3,0,10" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Derecho" Style="{StaticResource PropertyLabelStyle}" />
                                    <CheckBox Grid.Row="0" Grid.Column="4" Content="Con refuerzo" IsChecked="{Binding RightPostHasReinforcement, Mode=TwoWay}" Margin="0,3,0,10" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Tipo izquierdo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPostReinforcementCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Tipo derecho" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPostReinforcementCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Plates">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Placa izquierda" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPlateCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Placa derecha" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPlateCatalogId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Punto izquierdo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPlateConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Punto derecho" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPlateConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Horizontals">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="Las horizontales son piezas fisicas independientes; los paneles se recalculan entre horizontales consecutivas."
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,0,0,10" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="145" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="18" />
                                            <ColumnDefinition Width="145" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Horizontal" Style="{StaticResource PropertyLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedHorizontal.Label, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />

                                        <TextBlock Grid.Row="0" Grid.Column="3" Text="Elevacion" Style="{StaticResource PropertyLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedHorizontal.ElevationText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Perfil" Style="{StaticResource PropertyLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedHorizontal.ProfileId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                        <TextBlock Grid.Row="1" Grid.Column="3" Text="Cantidad" Style="{StaticResource PropertyLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedHorizontal.Quantity, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Cara" Style="{StaticResource PropertyLabelStyle}" />
                                        <ComboBox Grid.Row="2" Grid.Column="1" Style="{StaticResource PropertyComboBoxStyle}" ItemsSource="{Binding SideOptions}" SelectedItem="{Binding SelectedHorizontal.MountingFace, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                        <TextBlock Grid.Row="2" Grid.Column="3" Text="Estado" Style="{StaticResource PropertyLabelStyle}" />
                                        <ComboBox Grid.Row="2" Grid.Column="4" Style="{StaticResource PropertyComboBoxStyle}" ItemsSource="{Binding HorizontalStateOptions}" SelectedItem="{Binding SelectedHorizontal.State, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </Grid>
                                    <WrapPanel>
                                        <Button Style="{StaticResource CompactButtonStyle}" Content="Agregar horizontal" Click="AddHorizontal_Click" />
                                        <Button Style="{StaticResource CompactButtonStyle}" Content="Eliminar horizontal" IsEnabled="{Binding CanDeleteSelectedHorizontal}" Click="DeleteSelectedHorizontal_Click" />
                                        <Button Style="{StaticResource CompactButtonStyle}" Content="Duplicar horizontal" IsEnabled="{Binding CanDuplicateSelectedHorizontal}" Click="DuplicateSelectedHorizontal_Click" />
                                    </WrapPanel>
                                </StackPanel>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Connections">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Punto inicial panel" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.StartConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Punto final panel" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedBracingSegment.EndConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Placa izquierda" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding LeftPlateConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Placa derecha" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding RightPlateConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Panel activo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding SelectedSegmentSummary, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />
                                </Grid>
                            </Border>

                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource HiddenDetailPanelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveDetailKey}" Value="Other">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                        <ColumnDefinition Width="145" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Plantilla" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding TemplateSummary, Mode=OneWay}" IsReadOnly="True" Background="#F7F9FB" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Altura" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding HeightText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />

                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Fondo" Style="{StaticResource PropertyLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource PropertyValueTextBoxStyle}" Text="{Binding DepthText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </Grid>
                            </Border>
                        </Grid>
                    </StackPanel>
                    </ScrollViewer>
                </Border>

                <GridSplitter Grid.Row="1"
                              Height="8"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Center"
                              Background="#D8DEE6"
                              ShowsPreview="True"
                              ResizeBehavior="PreviousAndNext" />

                <Border Grid.Row="2"
                        Background="#F7F9FB"
                        BorderBrush="#D8DEE6"
                        BorderThickness="1"
                        Padding="12"
                        Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="Edicion rapida"
                                   FontWeight="SemiBold"
                                   Foreground="#1F2933"
                                   Margin="0,0,0,8" />
                        <WrapPanel>
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Agregar horizontal" Click="AddHorizontal_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Nuevo claro 44 in" Click="Add44Segment_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Nuevo claro 70 in" Click="Add70Segment_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Sin celosia" Click="QuickNoBracing_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Doble diagonal" Click="QuickDoubleBracing_Click" />
                        </WrapPanel>
                        <TextBlock Text="Herramientas de paneles y horizontales"
                                   FontWeight="SemiBold"
                                   Foreground="#1F2933"
                                   Margin="0,4,0,8" />
                        <WrapPanel>
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Eliminar horizontal" IsEnabled="{Binding CanDeleteSelectedHorizontal}" Click="DeleteSelectedHorizontal_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Duplicar horizontal" IsEnabled="{Binding CanDuplicateSelectedHorizontal}" Click="DuplicateSelectedHorizontal_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Dividir panel" IsEnabled="{Binding CanSplitSelectedSegment}" Click="SplitSelectedSegment_Click" />
                            <Button Style="{StaticResource CompactButtonStyle}" Content="Combinar paneles" IsEnabled="{Binding CanCombineSegments}" Click="CombineSelectedSegments_Click" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <GridSplitter Grid.Row="3"
                              Height="8"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Center"
                              Background="#D8DEE6"
                              ShowsPreview="True"
                              ResizeBehavior="PreviousAndNext" />

                <Border Grid.Row="4"
                        Background="#F7F9FB"
                        BorderBrush="#D8DEE6"
                        BorderThickness="1"
                        Padding="12"
                        Margin="0,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <DockPanel LastChildFill="True">
                                <TextBlock Text="{Binding SelectedSegmentCountLabel}"
                                           Foreground="#617080"
                                           FontWeight="SemiBold" />
                                <TextBlock DockPanel.Dock="Right"
                                           Text="{Binding SelectedBracingSegment.StatusLabel, StringFormat=Activo: {0}, TargetNullValue=Activo: ninguno}"
                                           Foreground="{Binding SelectedBracingSegment.StatusBrush}" />
                            </DockPanel>

                            <WrapPanel Margin="0,10,0,0">
                                <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                          Width="140"
                                          Margin="0,0,8,8"
                                          ItemsSource="{Binding PatternOptions}"
                                          SelectedItem="{Binding BulkPattern, Mode=TwoWay}" />
                                <Button Style="{StaticResource CompactButtonStyle}" Content="Aplicar arreglo" Click="ApplyBulkPattern_Click" />

                                <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                          Width="110"
                                          Margin="12,0,8,8"
                                          ItemsSource="{Binding SideOptions}"
                                          SelectedItem="{Binding BulkSide, Mode=TwoWay}" />
                                <Button Style="{StaticResource CompactButtonStyle}" Content="Aplicar cara" Click="ApplyBulkSide_Click" />

                                <TextBox Style="{StaticResource CellTextBoxStyle}"
                                         Width="230"
                                         Margin="12,0,8,8"
                                         Text="{Binding BulkProfileId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                <Button Style="{StaticResource CompactButtonStyle}" Content="Aplicar perfil" Click="ApplyBulkProfile_Click" />
                            </WrapPanel>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="Restaurar panel activo"
                                IsEnabled="{Binding CanRestoreSelectedSegment}"
                                Padding="12,7"
                                Margin="16,0,0,0"
                                VerticalAlignment="Top"
                                Click="RestoreSelectedSegment_Click" />
                    </Grid>
                </Border>

                <GridSplitter Grid.Row="5"
                              Height="8"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Center"
                              Background="#D8DEE6"
                              ShowsPreview="True"
                              ResizeBehavior="PreviousAndNext" />

                <Grid Grid.Row="6" Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="HorizontalTableRow" Height="0.95*" MinHeight="180" />
                        <RowDefinition Height="8" />
                        <RowDefinition x:Name="PanelTableRow" Height="1.15*" MinHeight="180" />
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="HorizontalsGrid"
                              Grid.Row="0"
                              ItemsSource="{Binding Horizontals}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              RowHeaderWidth="0"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              SelectionChanged="HorizontalsGrid_SelectionChanged"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Horizontal" Binding="{Binding Label}" IsReadOnly="True" Width="90" />
                            <DataGridTextColumn Header="Estado" Binding="{Binding StatusLabel}" IsReadOnly="True" Width="95" />
                            <DataGridTemplateColumn Header="Elevacion" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource CellTextBoxStyle}"
                                                 Text="{Binding ElevationText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Perfil" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource CellTextBoxStyle}"
                                                 Text="{Binding ProfileId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" Width="80" />
                            <DataGridTemplateColumn Header="Cara" Width="105">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                                  ItemsSource="{Binding DataContext.SideOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  SelectedItem="{Binding MountingFace, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Estado fisico" Width="125">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                                  ItemsSource="{Binding DataContext.HorizontalStateOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  SelectedItem="{Binding State, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Notas" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource CellTextBoxStyle}"
                                                 Text="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Row="1"
                                  Height="8"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  Background="#D8DEE6"
                                  ShowsPreview="True"
                                  ResizeBehavior="PreviousAndNext" />

                    <DataGrid x:Name="BracingSegmentsGrid"
                          Grid.Row="2"
                          ItemsSource="{Binding BracingSegments}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          SelectionMode="Extended"
                          SelectionUnit="FullRow"
                          SelectionChanged="BracingSegmentsGrid_SelectionChanged"
                          Margin="0,10,0,0"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White" />
                            <Setter Property="BorderBrush" Value="#E3E8EF" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsModified}" Value="True">
                                    <Setter Property="Background" Value="#FFF4D6" />
                                </DataTrigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#DCEBFF" />
                                    <Setter Property="BorderBrush" Value="#2B6CB0" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Panel" Binding="{Binding Index}" IsReadOnly="True" Width="60" />
                        <DataGridTextColumn Header="Estado" Binding="{Binding StatusLabel}" IsReadOnly="True" Width="95" />
                        <DataGridTemplateColumn Header="Horizontal inf." Width="115">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Style="{StaticResource CellTextBoxStyle}"
                                             Text="{Binding LowerHorizontalId, Mode=OneWay}"
                                             IsReadOnly="True"
                                             Background="#F7F9FB" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Horizontal sup." Width="115">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Style="{StaticResource CellTextBoxStyle}"
                                             Text="{Binding UpperHorizontalId, Mode=OneWay}"
                                             IsReadOnly="True"
                                             Background="#F7F9FB" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Inicio" Binding="{Binding StartElevation, StringFormat={}{0:0.##} in}" IsReadOnly="True" Width="80" />
                        <DataGridTextColumn Header="Fin" Binding="{Binding EndElevation, StringFormat={}{0:0.##} in}" IsReadOnly="True" Width="80" />

                        <DataGridTemplateColumn Header="Claro" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Style="{StaticResource CellTextBoxStyle}"
                                             Text="{Binding ClearHeightText, Mode=OneWay}"
                                             IsReadOnly="True"
                                             Background="#F7F9FB" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Arreglo de panel" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                              ItemsSource="{Binding DataContext.PatternOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding Pattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Cara" Width="105">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                              ItemsSource="{Binding DataContext.SideOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding SideMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Direccion" Width="135">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox Style="{StaticResource CellComboBoxStyle}"
                                              ItemsSource="{Binding DataContext.DirectionOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding DiagonalDirection, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Perfil" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Style="{StaticResource CellTextBoxStyle}"
                                             Text="{Binding BraceProfileId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Punto inicial" Width="145">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Style="{StaticResource CellTextBoxStyle}"
                                             Text="{Binding StartConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Punto final" Width="145">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Style="{StaticResource CellTextBoxStyle}"
                                             Text="{Binding EndConnectionPointId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>

            <GridSplitter Grid.Column="3"
                          Width="8"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="#D8DEE6"
                          ShowsPreview="True"
                          ResizeBehavior="PreviousAndNext" />

            <Border Grid.Column="4" Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="Vista previa vertical" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock Text="Esquema WPF para revisar altura, claros, celosia, postes y refuerzos."
                                   Style="{StaticResource MutedTextStyle}"
                                   FontSize="12"
                                   Margin="0,4,0,12" />
                    </StackPanel>

                    <Border Grid.Row="1"
                            BorderBrush="#D8DEE6"
                            BorderThickness="1"
                            Background="#FAFBFC">
                        <Canvas x:Name="PreviewCanvas"
                                Background="#FAFBFC"
                                SizeChanged="PreviewCanvas_SizeChanged" />
                    </Border>

                    <Grid Grid.Row="2" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Altura" Foreground="#617080" FontSize="11" />
                            <TextBlock Text="{Binding ConfiguredHeightText}" FontWeight="SemiBold" Foreground="{Binding HeightValidationBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Fondo" Foreground="#617080" FontSize="11" />
                            <TextBlock Text="{Binding Depth, StringFormat={}{0:0.##} in}" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>

                    <WrapPanel Grid.Row="3" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,10,4">
                            <Rectangle Width="11" Height="11" Fill="#2B6CB0" Margin="0,2,4,0" />
                            <TextBlock Text="Front" Foreground="#617080" FontSize="11" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,10,4">
                            <Rectangle Width="11" Height="11" Fill="#C53030" Margin="0,2,4,0" />
                            <TextBlock Text="Rear" Foreground="#617080" FontSize="11" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,10,4">
                            <Rectangle Width="11" Height="11" Fill="#805AD5" Margin="0,2,4,0" />
                            <TextBlock Text="Both" Foreground="#617080" FontSize="11" />
                        </StackPanel>
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>
        </ScrollViewer>

        <Border Grid.Row="2"
                Background="#E9EEF3"
                BorderBrush="#D8DEE6"
                BorderThickness="0,1,0,0"
                Padding="20,12">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{Binding StatusBrush}"
                           VerticalAlignment="Center" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Restaurar cabecera estándar"
                            MinWidth="190"
                            Padding="14,7"
                            Margin="0,0,8,0"
                            Click="RestoreStandardFrameButton_Click" />
                    <Button Content="Restaurar layout predeterminado"
                            MinWidth="185"
                            Padding="14,7"
                            Margin="0,0,8,0"
                            Click="ResetLayoutButton_Click" />
                    <Button Content="Insertar en AutoCAD"
                            IsEnabled="False"
                            MinWidth="140"
                            Padding="14,7"
                            Margin="0,0,8,0" />
                    <Button Content="Cerrar"
                            MinWidth="90"
                            Padding="14,7"
                            Click="CloseButton_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
